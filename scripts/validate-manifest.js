const fs=require('fs'); const path=require('path'); const root=path.resolve(__dirname,'..'); const m=JSON.parse(fs.readFileSync(path.join(root,'app-manifest.json'),'utf8')); let ok=true; for(const mod of m.modules){const p=path.join(root,mod.route); if(!fs.existsSync(p)){console.error('Missing route',mod.id,mod.route); ok=false}} for(const id of ['auth','seller-workspace','seller-profile','seller-readiness','product-access']){for(const f of ['index.html','module.json','README.md']){const p=path.join(root,'modules',id,f); if(!fs.existsSync(p)){console.error('Missing module file',p); ok=false}}} console.log(ok?'VALID_MANIFEST':'INVALID_MANIFEST'); process.exit(ok?0:1);
