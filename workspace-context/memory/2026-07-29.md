# 2026-07-29

## Tái cấu trúc Policy Cockpit → Customer Service Workspace (Health)

**Yêu cầu:** Làm lại trang chi tiết hợp đồng theo tư duy Banking Portal — RM-first, nghiệp vụ bảo hiểm chi tiết đẩy xuống lớp 2. 6 tab + thêm tab Chăm sóc khách hàng.

**Chốt với user (qua confirm gate):**
- Phạm vi: **Health trước**, duyệt rồi nhân ra Motor/PA.
- Endorsement: đổi tên tab "Yêu cầu dịch vụ" → "Sửa đổi bổ sung", giữ khung Service Request.
- Customer Care: **có log liên hệ**, lưu trong post-sale interaction store của Portal (KHÔNG đụng Customer Master).

**Kiến trúc giải pháp:** gate bằng `cfg.csWorkspace===true` trong `policy-cockpit.js` → nhánh `policyCockpitCS`. Motor/PA giữ nguyên đường cũ (`BANCA.POLICY_TABS` 6 tab cũ). Health dùng `BANCA.POLICY_TABS_CS`: Overview · Thu phí · Life Cycle · Sửa đổi bổ sung · Bồi thường · Chăm sóc KH.

**Deliverable:**
- `docs/rework-v2/F-policy-cockpit-cs-workspace.md` — BA spec 6 tab + business rules + acceptance criteria.
- `shared/components/policy-cockpit.js` — thêm §12 CS Workspace: `policyCockpitCS`, `pcThuPhi` (bảng kỳ + overdue đỏ), `pcLifecycle` (filter chips), `pcCare` (5 KPI + Next Best Action + contact log), `pcDrawer`/`pcOpenDrawer`, `pcComputeCare`, `pcNextBestActions`, CSS token-only (`pcCsStyles`), modal `openContactLog`/`submitContactLog`.
- `shared/mock/seed/post-sale.js` — thêm `contactLogs` store + `addContactLog` + `contactLogsOf` (Portal-owned, không ghi Customer Master).
- `modules/policies/index.html` — khối Health bật `csWorkspace:true` + `overviewCS` (benefitChips, benefitDetailHtml drawer, productInfoHtml drawer, paymentSummary, quickActions).

**QC:** node --check OK cả 3 file; validate-design-tokens 0 vi phạm mới ở file đã sửa (sửa 1 RAW_SHADOW → var(--shadow-3)); smoke test browser: Overview (chip quyền lợi + drawer + quick actions + payment summary), Care tab (5 KPI + contact log persist với timestamp/hẹn + badge count), Motor/PA không vỡ.

**Next:** chờ user duyệt Health rồi nhân overviewCS ra Motor (NCD/IDV) và PA. Rule Next Best Action hiện phụ thuộc dữ liệu billing (paidPct) — có thể tinh chỉnh khi có mock billing đầy đủ cho Health.

## Vòng tinh chỉnh 2 (feedback user 8.8/10) — 2026-07-29

User chấm 8.8/10, feedback 10 điểm. Ưu tiên #1 (must trước demo): **đổi 5 KPI hero từ Product → Policy Management**. Đã làm trọn gói:
- KPI hero: 🟢 Hiệu lực · 💰 Thu phí · 🧾 Bồi thường · 📝 Sửa đổi · 🔄 Tái tục (icon, suy từ pcComputeCare). Quyền lợi số (nội trú/ngoại trú/đồng chi trả) chuyển xuống "Tóm tắt quyền lợi" (benefitHighlights).
- Renewal card nổi bật trên Overview (+ nút "Đề nghị liên hệ khách" khi ≤120 ngày).
- Insured card gọn (tên+badge+N quyền lợi+"Xem chi tiết"→drawer/người).
- Parties đầy đủ 8 dòng (holder/insured/beneficiary/RM/branch/partner/source/renewal owner).
- Payment summary giàu: %/quá hạn + số tiền + ngày + phương thức + mã GD + installment.
- Quick action có icon (⬇📧📱📝🧾). Tab cao hơn (padding md/lg, text-md).
- Care snapshot rows (lần liên hệ/gọi/email/sms/tải GCN/hẹn/nhắc tái tục/next action).
- Core-sync badge góc phải pc-top (mock "Insurance Core · Đồng bộ 2 phút trước").

QC: syntax OK, validate-design-tokens 0 vi phạm mới, smoke test browser xác nhận 5 KPI + payment + parties + drawer/người + sync badge. pcCsStyles chỉ inject ở nhánh CS → Motor/PA không nhận CSS mới, không regression.
Advisor rate-limited cả 2 lần trong ngày.
