
## 2026-07-22 — Financial Advisory Workspace: full CR (P0 + P1 + P2) hoàn tất

**Bối cảnh:** Nâng Quick Advisory → "Financial Advisory Workspace". User chốt "full P0 + bỏ theo CR", rồi "làm tiếp p1 và p2".

### P0 (sáng 10:09 → 20:40)
- Seed `advice-sessions.js`: `NEED_QUESTIONS` (câu hỏi động per need: HEALTH bhyt/bệnh viện, INCOME thu nhập/reserve, FAMILY members/children, MOTOR value/use, ACCIDENT nghề), `needQuestionsFor`, `financialGapByNeed` (gap ĐỘNG theo need), `needToOfferGroup` (map need→nhóm sản phẩm — FIX bug lõi MOTOR ra Health), `budgetBand` map.
- Need Discovery: BỎ mode toggle; goal-cards + concerns + dynamic questions + budget; `window.advSetDynAnswer`.
- Financial Gap: info card (icon+rows+progress), KHÔNG table, đổi theo need.
- Recommendation hero: card fit cao nhất (isHero = fit===bestFit) viền teal + shadow + "⭐ BEST MATCH".
- Compare: toggle "Chỉ hiện điểm khác biệt" mặc định ON (`advToggleDiff`, fix init: `state.diffOnly=(state.diffOnly===false)`).
- Convert 3-tab modal: `advConvertTab('BANK'|'LEAD'|'NEW')` — Khách hàng ngân hàng (search) / Lead (search) / Tạo mới; `advAttachBank`/`advAttachLead`(carry leadId+campaign, mode REFERRAL)/`advAttachProspect`. app-workspace ctx fromAdvice carry leadId/campaign.
- BỎ theo CR: mode toggle, Save Draft ("Lưu để tư vấn sau" footer), Saved badge (#adv-save + footer ✓ Đã lưu).

### P1 UI (21:00)
- Stepper relabel + icon: `STEPS=[['needs','Nhu cầu','❤️'],['priority','Phân tích','📊'],['recommend','Gợi ý','⭐'],['compare','So sánh','⚖️'],['result','Quyết định','🎯']]`; stepper() render s[2] icon.
- Sidebar → Advisory Dashboard: metric rows (Nhu cầu chính/Ngân sách/Mức rủi ro/Financial Gap/Khuyến nghị/Fit Score).
- Section icons ở header các bước.

### P2 Business (21:00)
- `dataCompleteness()` → % + missing + progress bar; <60% → badge "Estimate".
- `riskLevel()` từ dynAnswers → Thấp/Trung bình/Cao.
- Eligibility: recommend card không đủ điều kiện hiện "Phù hợp: CÓ · Được phép bán: KHÔNG" + reason + nút "Chuyển seller đủ điều kiện" (`advTransferSeller` modal).
- Advisor Note structured: concern/budget/barrier/followup/free (`advSetAdvisorField` → tổng hợp vào advisorNote); state.advisorStruct.
- Follow-up task: `advFollowUp` modal (Ngày mai/Tuần sau/Tuỳ chọn date) → `advDoFollowUp` tạo task + toast + về quick-advisory?tab=FOLLOW_UP.

### Cache versions (CẬP NHẬT)
- module pages: `head-loader.js?v=42`
- internal: `V='v=20260722q'` (tiến trình phiên này: v=20260722l → m → n → o → p → q)

### Smoke PASS (browser targetId F6099CDAB65F66EAE354EFD6F1EDFC3A, localhost:8391)
- P0: no mode toggle + dynamic questions; gap dynamic INCOME (Quỹ dự phòng); hero BEST MATCH; convert 3-tab; lead attach carry leadId (mode REFERRAL); compare diff-only 10→13 khi tắt; no Save Draft.
- P1/P2: stepper relabel+icons (Nhu cầu/Phân tích/Gợi ý/So sánh/Quyết định); dashboard (risk metric); completeness 60%; structured note (#an-concern); follow-up modal 3 options.

### Files chính đã sửa
- `shared/mock/seed/advice-sessions.js` (NEED_QUESTIONS, financialGapByNeed, needToOfferGroup, adviceById merge localStorage)
- `modules/advisory-workspace/index.html` (rất nhiều: sidebar dashboard, stepper, gapCard, hero, compare diff, convert 3-tab, structured note, follow-up, transfer seller)
- `modules/quick-advisory/index.html` (list search + xoá nháp)
- `modules/application-workspace/app-workspace.js` (ctx fromAdvice carry leadId/campaign)
- `shared/js/head-loader.js` (V bump)

### CÒN LẠI (chờ user quyết)
- **Đề xuất cuối user**: chuyển wizard → workspace real-time 70/30 (Summary/Gap/Recommendation cập nhật ngay khi đổi answer, không cần bấm Tiếp tục). Re-architecture LỚN — đã hỏi user, chưa trả lời.
- Optional bỏ qua: customer-decision-different popup.

### LƯU Ý ĐƯỜNG DẪN
- Work thực tế ở `/Users/trixie/aicoworker/openclaw/workspace-bancassurance/` (KHÔNG phải openclaw path trong AGENTS.md). Prototype: `projects/sales-service-prototype/prototype/sprint1`.

## Update 10:40 — Transfer Seller: chuyển ra action row + modal danh sách seller
- BỎ nút "Chuyển seller đủ điều kiện" trong box đỏ eligibility (giữ text "Phù hợp: CÓ · Được phép bán: KHÔNG" + reason).
- Card không đủ điều kiện: action row thay nút "Chọn phương án" bằng secondary "Chuyển seller đủ điều kiện" (eligible thì vẫn là "Chọn phương án").
- `advTransferSeller(pkgRef)` → modal: section "⭐ Seller gợi ý" (score theo cùng team>branch>Retail RM) + "Danh sách seller đủ điều kiện" (ACTIVE, Retail RM/TL, ≠me, ≠serviceError, từ BANCA.personas). Mỗi seller nút "Chuyển" → advDoTransfer(id,name) → state.transferredTo + toast.
- Cache: V='v=20260722s'.
- Smoke (persona RM-02 BLOCKED health): boxBtns 0; 3 nút Chuyển seller ở action row; 0 nút Chọn phương án; modal có gợi ý + list (3 nút Chuyển). Persona đã khôi phục RM-01.
- Note test: readiness health — RM-01 READY, RM-02 BLOCKED (Thiếu mandatory health training), TL-01 READY, SVC-ERR SERVICE_UNVERIFIED. localStorage key persona: bancaPersona + banca_persona.

## Update 11:34 — Manager Workspace refactor capability-driven (4 slice, tự động)
User: "Chạy tuần tự cả 4, tự động". Refactor team-workspace theo profile/capability/channel, KHÔNG hard-code role.

**Slice 1 — seed `shared/mock/seed/manager-profiles.js`** (đăng ký head-loader sau case-meta):
- `DEFAULT_PROFILE` (seller PERSONAL) + `MANAGER_PROFILES` map lên persona có sẵn: RM-01 (Player-coach Portal SEGMENTED, sell, REASSIGN_IN/ASSIGN_TASK/COACH, commission), TL-01 (Telesales Supervisor Portal MANAGER, no sell, HYBRID), BM-01 (Bank Manager Embed MANAGER, MIRROR_READONLY, SYNCED, no commission).
- Helpers: `resolveManagerProfile`, `isManagerProfile`, `canDo(action)`, `channelMode`, `caseMutability`, `orgSourceOf`, `syncedAtOf`, `isEmbed`, `isReadonlyCase`, `inManagerScope`, `scopeLabel`, `sellersInScope`, `reassignActionLabel` (Portal="Chuyển người phụ trách" / Embed="Điều chỉnh ghi nhận"), `audit()`+auditLog (localStorage banca_audit).

**Slice 2-4 — viết lại `modules/team-workspace/index.html`** (component trung lập):
- Guard: !isManagerProfile → card PERSONAL redirect về seller-workspace.
- Header: label profile + scopeLabel + Portal/Embed chip + orgSource chip + syncedAt + time filter.
- Home theo homeLayout: PERSONAL (redirect), MANAGER (chỉ cockpit), SEGMENTED (tab "Công việc của tôi | Đội nhóm").
- Cockpit: exception-queue TRƯỚC KPI (severity HIGH/MED/LOW + typed action: task/coach/reassign/case/integration), KPI ≤6 (commission chỉ khi visibility.commission — chèn tile, cắt "Case cần hỗ trợ" để giữ 6), pipeline 7 stage click-drill, "Seller cần chú ý" top 5 (sellerRisk score).
- Team page (?view=team): full seller list + search + filter status + row click → detail; overflow menu action theo canDo.
- Seller detail (?view=seller): 5 tab (Tổng quan / Năng lực bán readiness ĐỘNG theo product / Hiệu suất / Hồ sơ / Coaching & task). Scope guard cho direct URL.
- Portal vs Embed: embedBanner mirror read-only; reassign modal Portal cảnh báo "đổi owner thực tế" vs Embed "chỉ điều chỉnh attribution, không đổi owner bank"; reason bắt buộc + audit.
- Privacy: mask mặc định + link "Yêu cầu xem PII" → modal reason + audit.
- Modals: task/coach/reassign(=re-attribution embed)/unmask — đều ghi BANCA.audit.
- Patch app-shell.js: `canSell` thêm điều kiện `resolveManagerProfile.sellingEnabled!==false` → TL-01/BM-01 ẩn CTA "Bắt đầu bán hàng" + "Tư vấn nhanh".

**Cache**: head-loader v42, V='v=20260722v'.

**Smoke PASS (4 persona)**:
- RM-01 SEGMENTED: 2 seg tab, exception trước KPI, commission hiện, Portal, reassign="Chuyển người phụ trách" + warning "owner thực tế", team 4 rows, seller detail 5 tab + readiness 3 product, unmask modal audit.
- TL-01 MANAGER: no seg tab, no personal pane, KHÔNG CTA bán hàng.
- BM-01 EMBED: mirror banner, Embed chip, "Đồng bộ lúc", no commission, reattr label="Điều chỉnh ghi nhận".
- RM-02 PERSONAL: guard redirect card.

**Còn (out of scope prototype)**: real org-sync engine, real audit store, backend reassign thật.

## Update 11:51 — Home = scope-tabbed workspace + Team 6-tab (bỏ Manager Workspace page độc lập)
- manager-profiles.js: thêm availableScopes+defaultScope (RM-01 [SELF,TEAM] def TEAM; TL-01 [TEAM] def TEAM; BM-01 [TEAM,BRANCH] def BRANCH) + helpers availableScopes/defaultScope/scopeTabLabel/membersForScope(scope)/appsForScope(scope).
- team-workspace VIẾT LẠI:
  - Home = scope-tabs từ availableScopes (Cá nhân/Đội nhóm/Chi nhánh). SELF→personalPane (việc của tôi + KPI cá nhân + readiness, tách khỏi team). TEAM/BRANCH→cockpitSummary: exception top5 + "Xem tất cả N việc" link, KPI scoped (cùng widget đổi phạm vi: "Premium đội nhóm/chi nhánh"), pipeline 7-stage click→cases filtered, seller cần chú ý top5, CTA "Xem toàn bộ <scope> →".
  - Home CHỈ summary+alert+shortcut; bảng đầy đủ chuyển sang Team page.
  - Team page (?view=team&scope=) = 6 tab: Tổng quan (team info + few KPI, không lặp cockpit) / Thành viên (list + search + filter status/readiness → seller detail) / Hồ sơ (filter status + stage + support + search) / Năng lực bán (MA TRẬN seller×product động) / Hiệu suất (perf table) / Công việc & Coaching (notes + pickSeller).
  - Deep-link: exception "Xem tất cả" → t=cases&filter=support; pipeline stage → t=cases&stage=X; readiness risk → t=selling&filter=risk; drill KPI support → cases filter.
  - Seller detail (?view=seller) 5 tab giữ. Portal/Embed + reassign/re-attribution + unmask + audit giữ.
- Cache: head-loader v42, V='v=20260722w'.
- Smoke PASS: RM-01 [Cá nhân,Đội nhóm] exception-first + CTA + no full table on home, SELF pane isolated; Team 6 tab + deep-link filter=support (3 rows) + matrix (Seller×3 product); BM-01 [Đội nhóm,Chi nhánh] default BRANCH + embed banner + KPI "Premium chi nhánh" + no commission.

## Update 12:01 — Scope pill dropdown khi ≥3 cấp
- manager-profiles.js: RM-01 availableScopes → ['SELF','TEAM','BRANCH'] (demo ≥3 cấp). inManagerScope() sửa dùng scope RỘNG NHẤT trong availableScopes (BRANCH>TEAM) để seller detail không chặn nhầm.
- team-workspace: USE_PILL=SCOPES.length>=3. Khi ≥3: hiện [Cá nhân][Quản lý] + pill "Phạm vi quản lý: <select> Team TEAM-A / Chi nhánh HCM01 ▼" (setManageScope → seg=MANAGE&mscope=). Khi ≤2: tab thường như cũ. homeMode(SELF/MANAGE)+manageScope; homeHref(scope) cho back-link. mgmtOrgLabel: TEAM→'Team X', BRANCH→'Chi nhánh Y'.
- Cache: V='v=20260722x'.
- Smoke: RM-01 (3 scope) → [Cá nhân][Quản lý]+pill (Team TEAM-A/Chi nhánh HCM01), đổi pill→BRANCH: KPI "Premium chi nhánh" + CTA scope=BRANCH. BM-01 (2 scope) vẫn tab thường [Đội nhóm][Chi nhánh], không pill.

## Update 13:10 — Thêm REGION (cấp thứ 4)
- manager-profiles.js: REGION_OF map branch→region (HCM01/HCM02/CALL/OPS/HCM99→'HCM') + regionOf(); membersForScope REGION = cùng region; inManagerScope broadest thêm REGION. RM-01 availableScopes → ['SELF','TEAM','BRANCH','REGION'] (demo 4 cấp).
- team-workspace: mgmtOrgLabel REGION → 'Khu vực '+regionOf. Pill giờ 3 mgmt option.
- Cache: V='v=20260722y'.
- Smoke RM-01: [Cá nhân][Quản lý] + pill 3 option (Team TEAM-A / Chi nhánh HCM01 / Khu vực HCM); chọn REGION → KPI "Premium khu vực" + CTA scope=REGION + membersForScope REGION=7 (branch=3, mở rộng đúng); Team page scope=REGION 7 member rows, back-link ?seg=MANAGE&mscope=REGION. Không lỗi.
- Kiến trúc scalable đã chứng minh: thêm cấp = thêm vào availableScopes + 1 dòng REGION_OF, KHÔNG xây tab/dashboard mới.

## Update 13:20 — Fix review 3 điểm nền + polish
FIX A (unified dataset — lỗi lớn nhất): aggregateKpi(scope) giờ DERIVE từ appsOf(scope) — policies=count ISSUED, premium=sum phí ISSUED, conversion=issued/submitted, sla=không breach/submitted, target=goal cấu hình. KPI reconcile với pipeline "Đã phát hành" (smoke: policies=1=pipeline issued=1). personalPane + teamOverview cũng dùng aggregateKpi. Thêm metricNote định nghĩa. appPremium() helper (top-level premium hoặc version CURRENT). => hết cảnh "KPI có số nhưng pipeline=0".
FIX B (Home scope tab): seller-workspace — bỏ block "Quick view Branch → Mở Manager Workspace". Player-coach (isManagerProfile + sellingEnabled) → Home prepend tab [Cá nhân(active)][Quản lý→team-workspace]. Manager-only (sellingEnabled=false: TL-01/BM-01) → Home location.replace sang team-workspace (Home CHÍNH LÀ cockpit, không dashboard cá nhân). Smoke: RM-01 home 2 tab + no quick view; TL-01 redirect team-workspace.
FIX C (Portal/Embed thực sự): exceptionsOf thêm embed integration exception (Callback thanh toán thất bại EXT-CASE-9082 nguồn JANUS_CRM + Đồng bộ lỗi). exAction: integration→[Retry sync][Xem tích hợp]; EMBED mọi case→[Xem mirror]. teamCases embed→openMirrorCase (Hệ thống nguồn/mã ngoài/khách mask/trạng thái sync/synced at/tham chiếu bank core://) read-only + retrySync (audit RETRY_SYNC). Không action sửa case ở Embed.
POLISH: exception item thêm cột Khách(mask)+Hạn/SLA+nguồn; empty state compact (~1 dòng thay khối lớn); header gọn 2 dòng VN ("Quản lý · <scope>" + chip "Nhúng/Cổng trực tiếp" + "Tổ chức đồng bộ" + "Cập nhật …", code EMBED/SYNCED chỉ trong title tooltip); period label "Tháng 07/2026" + Hôm nay/7 ngày/Quý III/Tùy chọn.
Cache: V='v=20260722z'.
Smoke PASS: reconcile TEAM (1=1); RM-01 home tabs; TL-01 redirect; BM-01 embed exception+retry sync+mirror modal read-only+header VN+period.
CÒN P1 (chưa làm, báo user): coaching workflow đầy đủ, reassignment rule engine, org sync reconciliation thật, trend comparison, bulk reminder, saved filter, export, loading/partial-error state gallery.

## Update 13:59 — Tab "Cá nhân" = không gian bán hàng thật
- team-workspace homeHref('SELF') → r+'modules/seller-workspace/index.html' (không còn pane tóm tắt ?seg=SELF). selfTab trong pill + simple tabs đều trỏ seller-workspace. personalPane thành dead code (giữ, vô hại). Loop nhất quán: seller-workspace [Cá nhân active][Quản lý→team] ↔ team-workspace [Cá nhân→seller][Quản lý active].
- Cache V='v=20260722za'. Smoke: Cá nhân href=seller-workspace.

## Update 14:22 — Luồng Handoff/Participant/Assignment (5 slice, tự động)
SLICE 1 — seed handoffs.js: PARTICIPATION_ROLE (referrer/advisor/selling_producer/case_owner/servicing/assignment_actor/supporter), HANDOFF_TYPE (SALES_HANDOFF/CASE_REASSIGNMENT/DELEGATION), HANDOFF_STATES 10, ROUTING_MODE, DECLINE_REASONS, referrerResolution config. 5 handoff seed (HO-001 sales pending-acceptance RM-01, HO-002 sales team-queue pending-assignment, HO-003 reassignment, HO-004 delegation, HO-005 auto-route unassigned-exception). Helpers: handoffsForSeller/pendingAssignment/createHandoff/assignHandoff/acceptHandoff/declineHandoff/reattribute (đều audit), participant getters, persist localStorage banca_handoffs_v1 (merge). acceptHandoff GIỮ referrer/advisor, chỉ THÊM producer/owner.
SLICE 2 — advisory convert customer-first 4 phần (advConvertConfirmModal): ① Khách hàng đã xác định (read-only) ② Tư vấn & participants (referrer/advisor mặc định theo người tạo) ③ Phương thức phân công (Tôi bán trực tiếp/Team queue/Auto) ④ Xác nhận. advDoConvert→createHandoff: DIRECT_SELF→accept ngay + app-workspace; TEAM_QUEUE→PENDING_ASSIGNMENT về quick-advisory; AUTO_ROUTE→UNASSIGNED_EXCEPTION.
SLICE 3 — seller-workspace tab Cá nhân section "Bàn giao mới" (handoffInbox): 3 loại badge (Chuyển sang bán hàng/Chuyển phụ trách/Công việc hỗ trợ) + nguồn (Bank CRM/Lead/Portal) + referrer/advisor/assignment_actor + hạn tiếp nhận + completeness. CTA accept/decline(reason bắt buộc)/review-customer(read-only). accept→acceptHandoff + điều hướng app-workspace/session.
SLICE 4 — team-workspace cockpit section "Chờ phân công" (TRƯỚC KPI): pendingAssignment(scope) bảng (trạng thái/khách+loại/tư vấn/ưu tiên+hạn/lý do) + nút Phân công → openAssign modal (customer READ-ONLY, chọn seller đủ điều kiện theo product readiness+workload, ghi assignment_actor, KHÔNG chọn lại customer). doAssign→assignHandoff.
SLICE 5 (lite) — team page BỎ tab "Hồ sơ" (→ Tổng quan/Thành viên/Năng lực bán/Hiệu suất/Công việc&Coaching). 
Cache: head-loader v42, V='v=20260722ze'.
Smoke PASS: model (3 handoff RM-01, pending 2, accept giữ referrer TS-01); seller inbox 3 loại + accept CTA; team queue TL-01 (trước KPI, 2 nút, assign modal customer read-only); convert TEAM_QUEUE→PENDING_ASSIGNMENT referrer giữ RM-01; team page no Hồ sơ tab.
CÒN (P1, báo user): team page restructure đúng [Thành viên][Hiệu suất][Coaching] (fold readiness vào Thành viên); Hồ sơ module thêm filter scope (Của tôi/Đội nhóm/Chi nhánh) + deep-link giữ filter; referrer resolution UI cho bank lead; NEED_MORE_INFORMATION sub-flow; re-rating khi đổi customer; audit viewer; SLA handoff vs application tách hiển thị.

## Update 14:34 — P1 theo ưu tiên (1-4)
P1-1 Hồ sơ module filter scope: submitted-applications + unsubmitted-applications thêm scope pill [Của tôi/Đội nhóm/Chi nhánh/Khu vực] (theo availableScopes), all=appsForScope(scope). isMgr derive = fScope!=='SELF' (LƯU Ý: trước xóa nhầm const isMgr gây trang trắng — đã fix). team-workspace pipeline/exception/drill deep-link giờ trỏ submitted-applications?scope=&group= (và unsubmitted cho Chưa nộp) thay t=cases. Smoke: scope=TEAM → 4 pill, Đội nhóm active, 13 rows.
P1-2 NEED_MORE_INFORMATION: handoffs.js BANCA.needMoreInfo(id,note). seller card thêm nút "Cần bổ sung" → modal note → state NEED_MORE_INFORMATION + banner "⏳ Đã yêu cầu bổ sung". Smoke PASS.
P1-3 SLA tách: seller card "SLA bàn giao · hạn tiếp nhận" + note "SLA hồ sơ tính sau khi tiếp nhận". PASS.
P1-4 Audit viewer: team-workspace footer link "Nhật ký kiểm toán" → openAudit modal (BANCA.auditLog 40 gần nhất: thời điểm/action VN/bởi/chi tiết previous→new+reason). PASS.
Cache: V='v=20260722zg'.
CÒN (P1 nhẹ, chưa làm): team page restructure đúng [Thành viên][Hiệu suất][Coaching] fold readiness; referrer resolution UI cho bank lead; re-rating khi đổi customer.

## Update 14:44 — P1 nhẹ (3 mục cuối)
1. Team page đúng spec: Tổng quan thành HEADER luôn hiển thị; tab còn [Thành viên][Hiệu suất][Công việc & Coaching] (default Thành viên); Năng lực bán (ma trận) FOLD vào tab Thành viên. Deep-link t=cases vẫn render (không phải tab). Smoke: 3 tab, overview header, matrix trong Thành viên.
2. Referrer resolution UI: advConvertConfirmModal ② thêm <select id=adv-referrer> (người tạo mặc định / theo lead nếu có / danh sách nhân viên NH / Không có) + hint nguồn (advRefHint). advDoConvert build participant REFERRER theo lựa chọn + source (CREATOR_DEFAULT/BANK_LEAD/MANUAL_BANK_EMPLOYEE/NONE→bỏ referrer). Smoke: 8 option, hint đúng.
3. Re-rating khi đổi customer: handoffs.js BANCA.changeCustomer(id,newCust,reason) → consent=PENDING, quoteStatus=RE_RATING_REQUIRED, nếu seller mới không đủ điều kiện product → PENDING_ASSIGNMENT + assignmentInvalidated; audit CHANGE_CUSTOMER. UI: hoReview modal thêm link "Yêu cầu điều chỉnh khách hàng" → hoChangeCust (chọn KH đúng + lý do bắt buộc). Smoke: quote RE_RATING_REQUIRED, consent PENDING.
Cache: V='v=20260722zh'.
=> Toàn bộ P1 handoff/assignment đã xong. Còn lại đều cần backend thật (auto-route engine, commission lock, integration contract Embed).

## Update 14:48 — Attribution xuyên suốt (Hồ sơ đã nộp + Hợp đồng)
- handoffs.js: PARTICIPANTS_SNAPSHOT (appId→referrer/advisor/sellingProducer/caseOwner/source/leadRef/branch/team/campaign; demo APP-101/102/104/105/107/110) + POLICY_DISTRIBUTION (policyId→+servicingSeller/channel/campaign/externalRef/effectiveDate; JB-POL-2026-0207/0184/2025-0102) + BANCA.participantsOf(id,ownerFallback)/policyDistributionOf(id,ownerFallback) (fallback owner khi không snapshot, cờ _fallback) + pName/pLine.
- app-workspace tracking: thêm card "Người tham gia giao dịch" (referrer/advisor/producer/owner + nguồn+lead + chi nhánh/team) TRƯỚC card Tóm tắt; card5 "Nguồn bán & attribution" bổ sung Kênh bán, dùng snapshot branch/lead/campaign.
- submitted-applications list: dưới khách hiện "👥 X giới thiệu · Y tư vấn" (khi khác người bán); cột sản phẩm thêm "Bán: <producer>"; filter drawer thêm "Người tham gia" (part=) lọc theo referrer/advisor/producer/owner.
- policies detail: section "Phân phối & người tham gia" (Người bán/GT/TV/Phục vụ hiện tại/Kênh·CN·Team/Nguồn·Campaign/External ref/Ngày hiệu lực) + ghi chú người bán≠người phục vụ.
- policies list: id cell subtitle "Bán: X · Phục vụ: Y"; filterBar thêm select "Mọi người tham gia" (part=).
- Quy tắc: reassign owner KHÔNG đổi referrer/advisor/producer (BANCA.acceptHandoff/assignHandoff giữ nguyên); snapshot giữ attribution sau submit/issue. changeCustomer đã có re-rating.
Cache: V='v=20260722zi'.
Smoke PASS: app 4 vai trò; submitted participant line + Bán; policy detail distribution+servicing+externalRef; policy list Bán subtitle + participant filter (5 rows).
CÒN cần backend thật: commission recalculation, approval workflow đổi người bán.

## Update 15:00 — Chỉnh hiển thị participant theo feedback ảnh
- App-workspace: GỘP người giới thiệu/tư vấn/bán/phụ trách vào chính card "Nguồn bán & người tham gia" (bỏ card riêng "Người tham gia giao dịch"); Partner+Chi nhánh·Team gộp 1 dòng.
- Policy detail "Phân phối & người tham gia": BỎ dòng "Người bán" (đã có ở "Định danh & các bên") + BỎ dòng "Kênh · Chi nhánh · Team". Giữ: GT/TV/Người phục vụ hiện tại/Nguồn·Campaign/External ref/Ngày hiệu lực. ("Người bán" chỉ còn trong ghi chú nhỏ.)
- Cache: V='v=20260722zj'. Smoke PASS.

## Update 15:03 — Refactor Đội nhóm: bỏ Coaching + gộp seller detail 1 trang
- BỎ COACHING khỏi Sales Portal: openCoach/doCoach/coachNotes, ma trận năng lực (teamSelling), tab Hiệu suất (teamPerf), tab Coaching (teamCoach), pickSeller coach, overflow COACH, exAction coach→case. Coaching thuộc LMS/HR.
- Home cockpit thêm "Hiệu suất thành viên" (bảng rút gọn 7 seller: Premium/Issued/Conversion/Target/Workload/Quá hạn/Chờ nhận, cùng dataset, click→seller detail) + CTA. At-risk đổi tên "Thành viên cần chú ý".
- Team page: bỏ tab Tổng quan/Hiệu suất/Năng lực/Hồ sơ/Coaching → còn [Thành viên][Công việc]. teamHeader hierarchy (Khu vực›Chi nhánh›Đội + QL trực tiếp/QL chi nhánh/nguồn tổ chức/thành viên/workload/quá hạn/chờ phân công — KHÔNG lặp KPI premium/conversion).
- teamMembers gộp cột: Nhân viên/Năng lực bán(R/C/B tooltip)/Workload/Chờ nhận/Premium/Conversion/Target/Cảnh báo/action. overflow bỏ Coaching, thêm "Xem hồ sơ đang xử lý".
- teamTasks (Công việc) mới: hợp nhất Chờ phân công + Chờ tiếp nhận + Việc đã giao + Quá hạn; sub-filter (Tất cả/Chờ phân công/Chờ tiếp nhận/Đã giao/Quá hạn); + Giao việc. Task store BANCA.tasks (thay coachNotes); openTask bắt buộc loại + gắn hồ sơ/khách.
- renderSeller: 1 TRANG tổng hợp (bỏ 5 tab): Header nhân viên (actions Giao việc/Chuyển/Xem hồ sơ, KHÔNG coaching) / Tình trạng bán hàng (compact, status VN "Có thể bán/Bán có điều kiện/Chưa thể bán", không ma trận) / Hiệu suất trong kỳ / Khối lượng công việc (4 ô click→module?scope=) / Công việc đang mở (3-5 + Xem tất cả) / Liên kết nghiệp vụ (shortcut hồ sơ/hợp đồng/handoff/hồ sơ NV). Không bảng hồ sơ raw.
- Fix status undefined/raw: personalPane + teamCases dùng BANCA.appStatusBadge; seller readiness dùng nhãn VN.
- LỖI đã fix: teamHeader thiếu scopeWord (ReferenceError → trang trắng) → thêm const scopeWord nội hàm.
- Cache: V='v=20260722zk'. Smoke PASS 4 mảng (home no-coach + perf table; team 2 tab + hierarchy + cột gộp; công việc sub-filter; seller detail 1 trang VN status no-undefined).

## Update 15:24 — Đội nhóm hierarchy-aware + count/tab + cột "Nhân viên & đơn vị"
- Org seed (manager-profiles.js): DEPT_OF_TEAM (TEAM-A/B→Phòng KHCN, TEAM-C→Phòng KHDN), deptOfTeam, orgPath(per) [Region›Branch›Dept›Team], scopeCommonDepth, memberOrgLabel(per,scope) (chỉ phần path KHÁC path chung của scope).
- COUNT mỗi tab (fix lỗi UI): nav tab "Thành viên (N)" / "Công việc (M)"; sub-filter chip Công việc "Tất cả (8)/Chờ phân công (2)/Chờ tiếp nhận (3)/Đã giao (0)/Quá hạn (3)". Refactor buildTaskRows() để đếm + render dùng chung.
- teamHeader redesign: tên đơn vị đúng (Đội TEAM-A / Chi nhánh HCM01 / Khu vực HCM), breadcrumb org từ orgPath, 2 nhóm CƠ CẤU (QL trực tiếp=teamMgr theo data / Thuộc / QL cấp trên=branchMgr / thành viên) vs VẬN HÀNH (workload/chờ phân công/quá hạn/chờ tiếp nhận). Phân biệt currentUser≠teamManager≠branchManager (tìm theo managerScope, không suy từ persona đăng nhập). Dòng "Người đăng nhập: …".
- teamMembers: cột gộp "Nhân viên & đơn vị" (tên/chức danh/mã + org-path rút gọn theo scope: TEAM ẩn, BRANCH hiện Phòng›Team, REGION hiện Chi nhánh›Phòng›Team). Readiness dạng VN "N có thể bán · N có điều kiện · N bị chặn" (code R/C/B chỉ trong tooltip). Cột: Tình trạng bán / Công việc (workload+quá hạn+chờ nhận) / Hiệu suất kỳ này / Target / Hoạt động / action.
- BỎ persona giả khỏi danh sách: Support Seller (role) + serviceError (Service Unverified) + manager-thuần không bán (sellingEnabled=false). Badge "Hỗ trợ tạm thời" (khác team) + "Quản lý đội · Có bán hàng" (manager có bán).
- Filter động: scope BRANCH/REGION + >1 team → hiện filter "Mọi đội" (team2=); scope TEAM không hiện. filterRows giữ khi chuyển tab (qua URL).
- Cache: V='v=20260722zl'. Smoke PASS: BM-01 branch (tab counts 2/8, title Chi nhánh HCM01, org path Phòng KHCN trong dòng, VN readiness, no fake persona, sub-chip counts); RM-01 team (title Đội TEAM-A, org path KHÔNG lặp per-row, 3 members).
- CÒN (spec lớn chưa làm hết): scope selector dạng cây org drawer; nhân viên đa đơn vị (chính + hỗ trợ) đầy đủ; filter phòng ban riêng; OrgUnit model tách file.

## Update 16:02 — OrgUnit model + scope selector cây + nhân viên đa đơn vị
- seed `shared/mock/seed/org-units.js` (đăng ký head-loader sau manager-profiles): ORG_UNITS explicit {id,name,type(REGION/BRANCH/DEPT/TEAM),parentId,managerId,source,attrs} cho HCM (Khu vực HCM › Chi nhánh HCM01 › Phòng KHCN › TEAM-A/TEAM-B; Phòng KHDN › TEAM-C; + HCM02, Call Center). Helpers: orgUnitById/orgChildren/orgSubtree/unitOfPersona/personaInUnit/rootUnitForScope/orgUnitLabelType. SUPPORT_ASSIGNMENTS {RM-01:[TEAM-B]} + supportUnitsOf.
- team-workspace: fUnit=qs.get('unit'); teamMembersList(scope) lọc theo personaInUnit + THÊM nhân viên hỗ trợ (support) khi xem unit TEAM (gắn _supportFrom). renderTeam members=teamMembersList → header/tab counts/list/tasks đều theo unit.
- teamHeader: nút "Phạm vi đang xem: <unit> ▼" (chỉ khi có unit con) → openScopeTree drawer (cây tổ chức đệ quy + đếm NV mỗi unit + "Toàn bộ <scope>"); selectUnit → ?unit=. Breadcrumb + tên đơn vị theo unit đã chọn. Banner "Đang lọc theo <unit> · xem toàn bộ".
- memberRow: badge "Hỗ trợ từ <team>" (nếu _supportFrom) hoặc "+N đơn vị hỗ trợ" (primary có support). org path rút gọn theo scope giữ nguyên.
- Cache: V='v=20260722zm'. Smoke PASS: BM-01 branch có picker + drawer cây (Phòng KHCN/TEAM-A/TEAM-B + NV counts); chọn TEAM-B → title "Đội TEAM-B" + banner lọc + RM-01 hiện badge "Hỗ trợ từ TEAM-A".
- => Đã hoàn tất toàn bộ spec hierarchy-aware trang Đội nhóm. Còn tùy chọn: OrgUnit sync engine thật, phân quyền unit ngoài scope (đã chặn theo rootUnitForScope), export cột org riêng.

## Update 16:17→16:30 — UX review trang chủ quản lý (cockpit) — triển khai 7 ưu tiên
User gửi bản đánh giá UX/UI trang chủ quản lý → chọn "Triển khai code prototype". Sửa team-workspace home + seed:
- Seed referrals.js BANCA.kpi enrich: prevPremium/targetPremium/prevConversion/prevPolicies cho RM-01/RM-02/TS-01/TL-01/BM-01 (demo data kỳ trước 06/2026).
- team-workspace: DEBUG flag (?debug=1) — chrome kỹ thuật (chip Nhúng/Cổng trực tiếp/Tổ chức thủ công, managerFootnote "Render theo...") chỉ hiện khi debug. NOW_TS='2026-07-22T16:20', relTime() (Còn X phút/Quá hạn X giờ/Chưa có hạn), fmtVNDate (22/07/2026, 15:00).
- Executive Summary bar (execSummaryBar): 5 ô việc cần xử lý/chờ phân công/mất quyền bán/%chỉ tiêu/HĐ phát hành.
- Trung tâm hành động (actionCenter): GỘP pendingAssignBlock + exceptionsOf → actionItems() schema chung, filter tab Tất cả/Quá hạn/Phân công/Quyền bán/Hồ sơ (setAC ?ac=), sort quá hạn→sev→hạn gần. pendingAssignBlock đã XÓA.
- KPI phân tầng (Hiệu quả kinh doanh): 3 card chính Premium/Hoàn thành chỉ tiêu (+gap VND, tpStatus icon+nhãn)/Hợp đồng phát hành + delta ▲▼ vs kỳ trước; hàng chẩn đoán Tỷ lệ chuyển đổi/SLA/Hoa hồng/Việc cần xử lý. aggregateKpi enrich targetPct/premiumDelta/policiesDelta/convDelta.
- Pipeline & điểm nghẽn: aging>3 ngày hoặc slaBreach → badge đỏ ⚠ per stage + banner "🚧 Điểm nghẽn: N hồ sơ kẹt ở X". Tách Chưa nộp.
- Hiệu suất đội ngũ: gộp bảng hiệu suất + "Thành viên cần chú ý" → 1 module mode [Cần chú ý (attn)/Toàn bộ] (setPM ?pm=), inline reasons + nút Giao việc.

## Update 16:36→17:20 — UX review portal RM/Seller 15 khu vực (P0→P3 full)
User chọn "Tất cả P0→P3". Đã làm:
- P0 app-shell.js: CTA '+ Bắt đầu bán hàng'→'+ Tạo hồ sơ bán hàng'; thêm nút '↩ Tiếp tục hồ sơ gần nhất' (draft mới nhất của me); modal title 'Tạo hồ sơ bán hàng — chọn ngữ cảnh'.
- P0 seller-workspace: actionQueueBlock() — summary chips hành động + bảng "Việc cần làm ngay" GỘP (bàn giao chờ tiếp nhận + cần bổ sung + quote hết hạn + có thể nộp), relDue sort, đặt TRƯỚC "Bàn giao mới". Bỏ khối "Ưu tiên hôm nay" cũ.
- P1 app-workspace REVIEW_AND_SUBMIT: blockers[] {t,fix,step} + banner đỏ "🚫 Chưa thể nộp hồ sơ" mỗi blocker có nút sửa trỏ đúng step (stepLink); banner xanh khi đủ điều kiện. Bỏ alert2 warn cũ.
- P1 unsubmitted: view 'need' mặc định (Cần tôi xử lý = mine + có quyền nộp + auth chưa đổi) khi scope SELF; nút Hủy → ovf menu ⋯ với confirm 2 bước (cancelApp); style .ovf trong file.
- P1 submitted: smart buckets viewChip need/uw/cust/done/sla/all (mặc định need khi SELF, sla đỏ khi >0); cột "Việc cần làm" lên vị trí 3; group pills chỉ hiện khi view=all.
- P2 thuật ngữ: Financial Gap→Khoảng thiếu bảo vệ, Fit Score→Độ phù hợp, Estimate→phí minh họa (advisory); Policy issued→Đã phát hành, Conversion→Tỷ lệ chuyển đổi, Target→Tiến độ chỉ tiêu (seller-workspace KPI + team-workspace seller perf + member table); Quote→Submit→Báo giá→Nộp.
- P2 nav nhóm (app-shell navItems): BÁN HÀNG (Tư vấn nhanh, HS chưa nộp) / XỬ LÝ (HS đã nộp) / SAU BÁN (Hợp đồng) / QUẢN LÝ (Đội nhóm).
- P2 policies list: tab NEAR_EXPIRY 'Sắp hết hiệu lực (≤45 ngày)' amber; header EN→VN (Số hợp đồng/Số GCN).
- P3 tokens.css cuối file: .btn min-height 40px, .btn-sm 34px (44px pointer:coarse), :focus-visible outline brand, bảng cuộn ngang chỉ @media ≤900px, badge nowrap.

## Update 17:04→17:30 — Policies scope + đồng bộ layout 3 bảng danh sách
- BANCA.policiesForScope(scope,me) trong manager-profiles.js. Policies list: SCOPES bar Của tôi/Đội nhóm/Chi nhánh/Khu vực (giữ scope qua tab), canView detail mở rộng inManagerScope.
- Đồng bộ layout 3 bảng (unsubmitted/submitted/policies): uxListHeader cho policies; row click cả dòng (tr onclick, id=span, action td stopPropagation); cột mã = mã + badge Janus Bank + tên owner khi isMgr.
- Rule cột Khách hàng (user chốt sau 2 vòng): cột Khách CHỈ tên + SĐT mask (BANCA.customerCell(cust) — không participant); cột riêng "Người bán" CHỈ tên seller (BANCA.sellerCell — bỏ dòng phụ referrer/advisor theo yêu cầu cuối); bỏ cột SĐT riêng ở unsubmitted; "Bán:" bỏ khỏi cột Sản phẩm. Helpers trong manager-profiles.js.

## Update 17:29→17:45 — UX review màn Đội nhóm → "bảng điều phối đội"
- renderTeam: 4 tab [Thành viên/Công việc/Quyền bán (badge đỏ N)/Hiệu suất]; title shell chỉ 'Đội nhóm' (bỏ mâu thuẫn 'Tổng quan khu vực · TEAM-A'); headerBar(ctxLabel) nhận label ngữ cảnh.
- teamHeader viết lại: summary strip 6 ô (thành viên·hoạt động/hồ sơ/chờ phân công/chờ tiếp nhận/quá hạn/quyền bán rủi ro) + mini action center (CTA Phân công→t=tasks&sub=assign; Xem quyền bán→t=auth; Xem & nhắc→t=tasks&sub=accept). Cơ cấu/Vận hành cũ đã bỏ.
- Picker "Phạm vi dữ liệu" LUÔN hiển thị: có đơn vị con → bấm được; không → disabled mờ + tooltip (yêu cầu user).
- QL trực tiếp/cấp trên tính theo ĐƠN VỊ ĐANG XEM (managerId của org unit, đi lên cây bỏ qua trùng tên khi 1 người kiêm nhiều cấp) — không theo persona đăng nhập. Fix "Phòng Phòng KHCN" lặp chữ (unitFullName).
- memberIssue(s): cột "Vấn đề chính" ưu tiên Ngừng HĐ>bị chặn quyền>quá hạn>tải cao(≥5 submitted)>conversion<20%>có điều kiện>chưa gán SP>Ổn định; bảng sort level giảm dần.
- Action tab Thành viên: THỐNG NHẤT nút "Xem chi tiết" mọi dòng (user yêu cầu, bỏ action theo issue); overflow ⋯ bỏ "Xem chi tiết" trùng, còn Xem hồ sơ đang xử lý/Giao việc/Chuyển.
- readinessBadges: [N Bán được][N Điều kiện][N Bị chặn] badge thay text. Filter rủi ro chips: Cần chú ý/Bị chặn quyền bán/Tải công việc cao/Không hoạt động (?risk=, bấm lần 2 bỏ).
- teamAuth (tab Quyền bán): ma trận seller×product badge + lý do, dòng đỏ khi B>0. teamPerf (tab Hiệu suất): bảng KPI đầy đủ.
- openScopeTree: mỗi unit hiện "N người · N hồ sơ"; title "Sơ đồ tổ chức / Phạm vi dữ liệu".
- Chuẩn giá trị rỗng: hết undefined/—; dùng Chưa có hạn/Chưa phân công/Không phát sinh/Chưa có dữ liệu/Không áp dụng/chưa đặt.

### Trạng thái test cuối phiên
- Mọi module parse OK (node -e new Function extract script), console sạch. Browser targetId 26040CDEBEBD892037DE6FEDEE70040E (file:// trực tiếp, không cần localhost). Persona localStorage 'bancaPersona' — đã reset RM-01.
- Test pattern: TL-01 = chỉ TEAM (picker disabled); BM-01 = TEAM+BRANCH manager thuần (không SELF); RM-01 = 4 scope player-coach.
