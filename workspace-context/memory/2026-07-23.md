# 2026-07-23 — Banca Sales Portal: Re-architecture multi-product journey (spec final)

## Bối cảnh
User gửi spec final (19 mục): chuyển prototype từ Motor-hardcode sang nền tảng multi-product sales journey. 2 vertical slice bắt buộc: Motor Comprehensive (có thẩm định) + Personal Accident (straight-through). User chốt: **Checkpoint theo slice** + Payment **mô phỏng UI states**.

## Kế hoạch: PLAN-81422494.md / TODO-81422494.md
Thứ tự P0.1→P0.6 (foundation trước, demo, rồi 2 slice, rồi continuity).

## ĐÃ XONG — Foundation P0.1-P0.3 (logic layer, checkpoint)

### File mới: `shared/mock/seed/journey-registry.js` (P0.1)
- `BANCA.ENTRY_MODES` (7 mode) + `buildSalesEntryContext(raw)` chuẩn hóa entry.
- `BANCA.ProductJourneyDefinitions` {motor, pa}: riskObjectType, quickRatingSchema, declarationSchema, packageSchema, documentRuleSet, ratingStrategy, underwritingMode(RULE_BASED/STP), paymentMode, issueMode, reviewLayout, hiddenStages, stages[].
- Helpers: `journeyFor / journeyEditStages / journeyAllStages / journeyStageLabel`.
- QuoteSnapshot (`makeQuoteSnapshot`, QUOTE_KIND INDICATIVE/FIRM), ApplicationRoutingResult (`ROUTING` 4 code + `makeRoutingResult`), Payment (`PAYMENT_STATUS` 8 + `makePayment`/`paymentBadge`), Issue+Policy (`ISSUE_STATUS`, `makePolicy`, `genPolicyNo`/`genCertNo`), `demoMode()`.

### File mới: `shared/mock/seed/product-schemas.js` (P0.2 data + P0.3 logic)
- PA schemas MỚI: `paPackages` (PA_BASIC/STD/PLUS), `paOccupationClasses` (4 class), `paAgeBands`, `ratePA()` = STBH/1e6 × rate(ageBand) × occFactor + VAT; `paRiskQuestions`, `paDocuments`.
- `motorRiskQuestions` structured (claimCount/commercialUse/floodExposure/modifiedVehicle/previousLossAmount).
- `motorRiskRating(riskAnswers)` → lines(loading) + flags(referral/requireDoc/reject/conditions).
- Dispatcher `rateProduct(productId,inputs)` theo ratingStrategy.
- Quote lifecycle: `computeIndicativeQuote` (phí dự kiến) + `computeFirmQuote` (riskAnswers vào rating → version mới + diff chênh lệch).
- `evaluateUnderwriting(app)` → routing (Motor RULE_BASED / PA STP).

### Tích hợp tối thiểu app-workspace.js
- Stepper chuyển REGISTRY-DRIVEN: `steps = BANCA.journeyEditStages(app.productId)` (thay `BANCA.STAGES.filter`). AUTO_STAGES lấy từ `journeyFor().hiddenStages`. Motor giữ 6 bước (ẩn INSURED_PARTY) → KHÔNG regression.

### head-loader
- Thêm journey-registry.js (sau status-model), product-schemas.js (sau vehicle-master). Cache V='v=20260723b'.

### Smoke PASS (node + browser targetId 56DD1D36...)
- Motor indicative 9.575.500đ → firm 11.525.500đ (+20%: HYDRO_LOCK 6% + CLAIM_HIST 10% + COMMERCIAL 15%). Router 2claim+comm → UW_REQUIRED; clean → APPROVED_FOR_BIND.
- PA rating 534.600đ (STD, age35, CLASS_2). Router clean → APPROVED_FOR_BIND; seriousIllness → REJECTED.
- Browser: no console error; journeyEditStages motor 6 / pa 5 đúng.

## CHỜ USER duyệt checkpoint → tiếp P0.4 Motor slice (wiring UI: firm quote diff, UW pending, approve-with-condition, customer confirm, payment, issue).

## LƯU Ý ĐƯỜNG DẪN
- Work thực tế ở `/Users/trixie/aicoworker/openclaw/workspace-bancassurance/` (KHÔNG phải path trong AGENTS.md). Prototype: `projects/sales-service-prototype/prototype/sprint1`.

## Update 11:56→ — HARDENING foundation (theo review chi tiết user, 4 nhóm)
User duyệt hướng nhưng yêu cầu hardening trước P0.4. Đã làm hết 4 nhóm:
- **G1 contracts đầy đủ** (journey-registry.js rewrite): QuoteSnapshot (quoteId/quoteVersion/quoteType/quoteStatus DRAFT-VALID-STALE-EXPIRED-SUPERSEDED-ACCEPTED-CANCELLED/premiumBreakdown{base,addOn,loading,discount,tax,fee,total}/ratingInputsSnapshot/supersedesQuoteId/inputHash). ApplicationRoutingResult (blockingIssues/requiredDocuments/premiumChanged/customerConfirmationRequired/nextAction/internalReasonCode vs displayReason/displayable/generatedAt). IssueResult TÁCH khỏi Policy (makeIssueResult). Payment (paymentId/applicationId/externalTransactionId/paymentUrl/idempotencyKey/createdAt/updatedAt). canCreatePayment(routingCode).
- **G2 source payload**: SalesEntryContext giữ originalPayload + sourceReference{externalCustomerId/ReferralId/PolicyId/CaseId} + createdAt; recommendedProductId tách selectedProductId. Enum 7 mode chính thức (ENTRY_MODE_ENUM) thêm QUICK_ADVICE_CONVERSION; INSURANCE_CUSTOMER là alias→BANK_CUSTOMER. Journey def thêm productVersion/journeyVersion/customerType/schemaIds/reviewSections/certificateTemplate/supportedEntryModes/effectiveFrom-To; stages có component+readonlyAfterSubmit; journeyStageComponent/journeySupportsEntryMode.
- **G3 rating hardening** (product-schemas.js): ratingStrategy versioned (MOTOR_RATING_V1/PA_TABLE_RATE_V1). PRICING_SOURCE='DEMO_TARIFF' + breakdown trong rating. validatePA (age 0-65/nghề CLASS_4 loại trừ/STBH≤1 tỷ/existingDisability+seriousIllness chặn/hazardous+buyer≠insured cảnh báo). computeFirmQuote: structured adjustments {code,label,type,percentage,amount}, supersedes + mark indicative SUPERSEDED, no-dup guard (inputHash → unchanged=true). modifiedVehicle → referral KHÔNG loading tự động (pct=null skip).
- **G4 test suite**: scripts/test-foundation.js — 58 test PASS (cross-product contamination, quote versioning, no-dup, resume PA, 7 entry modes, router↔payment eligibility, backward-compat, demoMode). `node scripts/test-foundation.js`.
- Cache V='v=20260723c'. Browser (targetId 56DD1D36...) reload sạch, 0 console error; motor 6 / pa 5 steps, firm delta +20%, router UW_REQUIRED.

## CHỜ user duyệt hardened foundation → P0.4 Motor UW slice (UI CHỈ đọc registry/router, không hard-code productId).

## Update — P0.4 Motor slice CORE (router + continuity + firm quote + registry risk declaration)
- **submitApp REWIRE** (app-workspace.js ~801): dùng `BANCA.evaluateUnderwriting()` → set status=routing.appStatus + lưu app.routing; continuity: `location.href='?id=..&tab='+tabByStage[nextStage]+'&routed=1'` (PAYMENT→confirmpay, UNDERWRITING→uw, REVIEW→supplement). KHÔNG còn location.href về submitted-applications list (fix spec §9).
- **RISK_DECLARATION registry-driven** (~523): render câu hỏi từ `BANCA.riskQuestionsFor(productId)` (motor 5 câu / pa 3 câu), không hard-code. `window.setRiskAnswer(id,code,val,kind)` lưu app.riskAnswers qua patchApp + refresh panel.
- **firmQuotePanelHtml(app)**: Motor → computeFirmQuote từ inputsSnapshot + riskAnswers, hiển thị diff structured (phí dự kiến → +từng loading (label+%+tiền) → phí chính thức → chênh lệch %) + cảnh báo referral+requireDoc. PA → validatePA precheck (eligible/warnings). Note DEMO_TARIFF. Initializer setTimeout điền panel sau khi handlers sẵn sàng.
- `BANCA.riskQuestionsFor(productId)` thêm vào product-schemas.
- **Smoke browser PASS** (DRAFT-2026-005 motor, targetId 56DD1D36): 3 radio (commercialUse/floodExposure/modifiedVehicle) + 2 number (claimCount/previousLossAmount); set claimCount=2+commercialUse=true → panel "Phí dự kiến 10.560.770 → +Lịch sử tổn thất 10% +910.000 → +KD vận tải 15% +1.365.000 → Phí chính thức 12.835.770 (+22%) + ⚠ kích hoạt THẨM ĐỊNH". Router → UW_REQUIRED/nextStage UNDERWRITING/canPay=false. Console 0 error. 58 test vẫn PASS.

## CÒN P0.4 (chưa làm): payment mock UI states (QR/link/retry/expired/callback), approve-with-condition→customer confirm screen, issue mock (BINDING→ISSUING→ISSUED số HĐ+GCNBH), verify tracking tabs uw/confirmpay/payment/policy đọc app.routing. Rồi P0.5 PA slice, P0.6 continuity resume, terminology+tooltip.

## Update 12:19 — P0.4b post-submit UI (nút demo từng bước, user chọn PA "1")
- Tab UW: panel "🎬 Mô phỏng kết quả thẩm định" (Duyệt/Duyệt có điều kiện/Từ chối) — chỉ hiện khi !app.uw && status∈[PENDING_RECEIPT,PENDING_UW,IN_UW] && owner==me. Hiện risk conditions từ app.routing.
- `window.simulateUw(decision)`: REJECTED→status REJECTED+uw reason (chỉ lý do chia sẻ được). APPROVED→PENDING_PAYMENT (không cần confirm)→confirmpay. APPROVED_WITH_CONDITION→UW_DECIDED, tính newPremium qua computeFirmQuote từ risk answers, loading/condition/reason (không lộ note nội bộ).
- `window.simConfirm()`: PENDING_CUSTOMER_CONFIRM→PENDING_PAYMENT (otp VERIFIED). Nút "✓ Khách đã xác nhận (demo)" ở tab confirm state SENT.
- Tái dùng handler cũ: sendConfirm (UW_DECIDED→gửi link), payDemo QR (→ISSUED+addPolicyDemo), payRetry.
- **Smoke E2E PASS** (DRAFT-2026-005): setRiskAnswer(claim2+commercial)→submit→PENDING_UW+simPanel→simulateUw(có điều kiện)→UW_DECIDED newPremium 12.835.770(+22%)→sendConfirm→PENDING_CUSTOMER_CONFIRM→simConfirm→PENDING_PAYMENT→payDemo QR→ISSUED policy JB-POL-2026-0430. Console 0 error. 58 test PASS.
- => Motor Comprehensive underwriting flow (spec §7.1) chạy đủ: phí dự kiến→khai báo rủi ro→phí chính thức→thẩm định có điều kiện→khách xác nhận→thanh toán→phát hành.

## TIẾP: P0.5 PA straight-through (journey riêng→auto approve→payment→auto issue). Rồi P0.6 continuity resume + verify tracking đọc routing. Rồi terminology+tooltip+list CTA.

## Update — P0.5 PA straight-through slice DONE
- app-workspace.js: thêm block INSURED_PARTY (form người được BH: họ tên/tuổi/nhóm nghề/buyer≠insured — KHÔNG field xe) + block PACKAGE_AND_QUOTE nhánh PA (paPackages + ratePA, phí dự kiến). Dispatch theo app.productId (registry-driven).
- Handlers: paSetField(id,field,val,kind) lưu insuredAge/occupationClass/buyerIsInsured; paPickPackage(id,code) → computeIndicativeQuote('pa') set app.quote+package+sumInsured+premium.
- Seed: thêm DRAFT-2026-PA1 (owner RM-01, productId pa, insuredAge 35, CLASS_1). Cache V='v=20260723d'; index.html app-workspace.js?v=20260723d (LƯU Ý: app-workspace.js có version RIÊNG trong index.html — phải bump cả 2).
- **Smoke E2E PASS** (DRAFT-2026-PA1): PA stepper 5 bước (Khách/Người được BH/Gói&báo giá/Khai báo/Review), form người KHÔNG rò field xe (AC01). paPickPackage PA_STD→phí 396.000₫. submit→router APPROVED_FOR_BIND→PAYMENT canPay=true (AC05, không qua UW/confirm)→payDemo QR→ISSUED policy JB-POL-2026-0187. Console 0 error.
- Note nhỏ: payDemo dùng số HĐ generic JB-POL (không JB-PA) — đủ demo, có thể polish sau.

## TIẾP: P0.6 continuity resume (verify tracking tabs đọc app.routing, resume đúng stage sau thoát giữa chừng) + terminology chuẩn hóa + demoMode tooltip ⓘ + list CTA động.

## Update — P0.6 continuity + terminology + demoMode tooltip (mục cuối)
- **P0.6**: continuity core đã có (submit route đúng tab, không về list). Thêm routedBanner (qs.routed=1 && app.routing): hiển thị "✓ Đã gửi yêu cầu bảo hiểm — kết quả: <label>" + reasons + conditions + next action. Verify PASS (Motor "Cần thẩm định").
- **Terminology §11**: nút submit "Nộp hồ sơ"→"Gửi yêu cầu bảo hiểm"; banner "Chưa thể nộp hồ sơ"→"Chưa thể gửi yêu cầu bảo hiểm"; "đủ điều kiện nộp"→"đủ điều kiện gửi".
- **demoMode tooltip §14**: `BANCA.demoMetaTooltip(app)` (ⓘ 300px, hover/focus, aria-label, ≤4 dòng, ẩn khi !demoMode). demoMeta thêm vào DRAFT-2026-005 (Motor cần thẩm định) + DRAFT-2026-PA1 (PA phát hành nhanh). Wire vào unsubmitted-applications product cell. Verify: demo=1 → 2 tooltip đúng nội dung; demo=0 → 0 tooltip. Console sạch. 58 test PASS.
- Cache V='v=20260723f' (head-loader + index.html app-workspace.js).
- LƯU Ý test: reset overlay bằng xóa localStorage key /banca.*(overlay|app|store)/ (giữ bancaPersona/bancaLang) khi draft demo bị submit ở lần test trước.

## ===== TOÀN BỘ P0 (P0.1→P0.6) + terminology + tooltip HOÀN TẤT =====
2 vertical slice chạy end-to-end:
- Motor Comprehensive: phí dự kiến → khai báo rủi ro (registry) → phí chính thức +diff → gửi → UW_REQUIRED → duyệt có điều kiện → khách xác nhận → thanh toán → phát hành.
- PA straight-through: journey riêng (không field xe) → chọn gói → gửi → APPROVED_FOR_BIND → thanh toán → auto-issue.
Foundation hardened (contracts đầy đủ, 58 test). AC01-AC08 cơ bản đạt. CÒN (polish/P1): số HĐ PA-branded, payment mock states chi tiết hơn (QR/link/retry UI cho hồ sơ router-created), wire tooltip vào submitted list, renewal prefill, review layout theo reviewSections.

## Update 12:37 — Pass P1 polish (user "tiếp") — 5 mục XONG
- **P1-a** số HĐ/GCNBH PA-branded: payDemo/payRetry dùng genPolicyNo(productId)/genCertNo(productId) → PA ra JB-PA-.../GCN-PA-...
- **P1-b** registry dispatch purity: PACKAGE_AND_QUOTE nhánh PA dùng journeyStageComponent()==='paPackage' thay app.productId==='pa'; firmQuotePanelHtml dispatch theo journey.underwritingMode==='STP'; computeFirmQuote(a.productId) thay hard 'motor'.
- **P1-c** renewal prefill §12: ?renew=<policyId> → ctx RENEWAL prefill vehicle/idv/package/mortgage/prevPremium, mở ở RISK_OBJECT, banner "♻ Tái tục từ HĐ X — xác nhận thay đổi + re-rate". Smoke: JB-POL-2026-0207 → banner + xe Honda Civic + step Xe.
- **P1-e** review REGISTRY-DRIVEN theo journey.reviewSections + blockers registry-aware. FIX BUG cross-product: review cũ hard-code row "Xe" + blocker "Chưa nhập thông tin xe" → PA KHÔNG submit được qua UI. Nay: PA review có "Người được bảo hiểm", KHÔNG row Xe, không blocker xe, canSubmit=true; Motor vẫn có Xe (no regression). PA blocker dùng validatePA precheck.
- **P1-d** §10 chống thanh toán trùng (idempotency): payDemo/payRetry guard nếu ISSUED/payment SUCCESS → chặn; chỉ cho thanh toán khi status PENDING_PAYMENT/PAID (seller không tự đánh dấu).
- Cache V='v=20260723i'. 58 test PASS. Console 0 error mọi smoke.

## ===== P0 + P1 polish HOÀN TẤT. Prototype = multi-product sales journey platform thực sự (Motor UW + PA STP end-to-end, registry-driven, contracts đầy đủ, renewal, terminology, demo tooltip). =====

## Update 12:53 — Rà soát & sửa luồng sau submit (STP UW + payment method selection) — 6 mục XONG
- **F1 Foundation**: journey def thêm underwritingUnit (Motor UW Team/queue Motor vs "Thẩm định tự động (STP Engine)" + ruleSetVersion). status-model thêm APP_STATUS.PAYMENT_METHOD_REQUIRED. journey-registry: PAYMENT_STATUS thêm METHOD_REQUIRED; PAYMENT_CHANNELS (QR/PAYMENT_LINK/CARD/BANK_ACCOUNT/BANK_TRANSFER) + PAYMENT_EXPERIENCES (CUSTOMER_REMOTE/PRESENT/SELLER_ASSISTED); makePayment mở rộng (paymentChannel/experience/initiator/payerType/payer/recipient/qrPayload/gatewayReference/currency); makeStpDecision(productId) (decisionCode/ruleSet/unit theo product).
- **F2 submitApp STP**: product underwritingMode=STP + APPROVED_FOR_BIND → tạo stpDecision + status PAYMENT_METHOD_REQUIRED (KHÔNG PENDING_PAYMENT), payment=null, route tab uw. Manual/motor giữ UW_REQUIRED.
- **F3 UW tab STP variant**: nếu app.stpDecision → card "Đã chấp thuận tự động" (đơn vị/mã UWD/timestamp/rule set/phụ phí/loại trừ/điều kiện/quyền thanh toán) + CTA "Chọn cách thanh toán". KHÔNG queue/officer/SLA/Motor UW Team. Manual giữ wsCard cũ.
- **F4 Payment method selection**: openPaymentMethod modal 3 nhóm (A QR tại quầy/B payment link gửi khách/C seller hỗ trợ) → createPaymentIntent(channel,experience) tạo intent PENDING (chỉ khi đó mới PENDING_PAYMENT). Payment tab: PAYMENT_METHOD_REQUIRED→card "Đã chấp thuận · Chờ chọn cách" + CTA; chưa duyệt→KHÓA (🔒 + lý do); PENDING→hiện QR/link+recipient + settlePayment(SUCCESS/FAILED/EXPIRED) callback (seller KHÔNG tự đánh dấu) + recreatePaymentIntent khi fail/expired. simulateUw APPROVED + simConfirm giờ set PAYMENT_METHOD_REQUIRED (thống nhất Motor+PA).
- **F5 Fix bugs**: quoteRef "undefined-V1" → fallback quoteId/quoteVersion. casePhase+caseNextAction+getSubmittedCaseActions map PAYMENT_METHOD_REQUIRED (CTA "Chọn cách thanh toán"). History: chỉ log "Khởi tạo yêu cầu thanh toán" khi có intent+paymentInitAt; thêm event STP approved + "Chờ chọn cách thanh toán"; empty state payment = KHÓA (không "Chờ thanh toán" sớm). PA không map Motor UW Team.
- **Smoke E2E PASS** (targetId 56DD1D36):
  - PA STP: paPickPackage→submit→PAYMENT_METHOD_REQUIRED+stpDecision(unit "Thẩm định tự động", payment=null); tab uw "Đã chấp thuận tự động" KHÔNG Motor team/queue + mã UWD; createPaymentIntent(QR)→PENDING→settlePayment(SUCCESS)→ISSUED policy JB-PA-2026-8157.
  - Motor manual: submit→PENDING_UW(sim panel)→duyệt điều kiện→confirm→PAYMENT_METHOD_REQUIRED(CTA)→createPaymentIntent(PAYMENT_LINK)→settle SUCCESS→ISSUED JB-MT-2026-8096.
  - Không còn undefined-V1; console 0 error; 58 test PASS.
- Cache V='v=20260723j'.

## Update 12:5x — self-review fix (advisor unavailable)
- State machine gap: PAYMENT_METHOD_REQUIRED chưa có trong 4 list "post-confirmation" [PENDING_PAYMENT,PAID,PENDING_ISSUE,ISSUED] → tab Xác nhận/hiển thị tiến độ sai. Đã thêm PAYMENT_METHOD_REQUIRED vào cả 4 (confirm cState, quote/payment progressed checks). Cache V=v=20260723k. 58 test PASS, console sạch.

## Update 13:06 — 3 nhóm non-backend còn lại (§11/§15/§16/§13) XONG
- §11 terminology: app-shell nav "Yêu cầu chưa gửi"/"Yêu cầu đã gửi"; nút "+ Bắt đầu bán bảo hiểm"; modal title; app-workspace shell titles; app-manifest; module.json; 5 module HTML (unsubmitted/submitted/seller/team/help). Hết "Hồ sơ chưa/đã nộp".
- §15 CTA động (unsubmitted): stageCta labels khớp spec (Tiếp tục nhập khách hàng/thông tin xe/Tính phí dự kiến/Tiếp tục khai báo/Bổ sung tài liệu/Review và gửi) + "Tính phí lại" khi quote warning. (continueBtn đã có sẵn).
- §16 CTA động (submitted): cta(a) khớp bảng §16 (Theo dõi thẩm định/Bổ sung thông tin/Xác nhận điều kiện/Chọn cách thanh toán/Thanh toán/Thử lại thanh toán khi payment FAILED/EXPIRED/Theo dõi phát hành/Xem hợp đồng) + thêm PAYMENT_METHOD_REQUIRED.
- §13 data demo: thêm DRAFT-2026-RN1 (RM-01 Motor tái tục, prefill Honda Civic + renewalPolicyRef JB-POL-2026-0207 + demoMeta). RM-01 giờ demo 3 scenario: Motor cần thẩm định (005) + PA phát hành nhanh (PA1) + Motor tái tục (RN1).
- Cache V=v=20260723l. Smoke PASS: nav labels mới, CTA động unsubmitted (Tiếp tục nhập khách hàng/Review và gửi/Tính phí lại) + submitted (Bổ sung thông tin/Thanh toán/Xác nhận điều kiện), 3 tooltip scenario, renewal banner RN1. 58 test PASS, console sạch.

## => Toàn bộ phần NON-BACKEND của spec 19 mục đã hoàn tất. Còn lại chỉ nhóm cần backend thật.

## Update 13:33 — Re-architecture state model + canonical resolver (spec nghiêm ngặt DEF-001..005)
- R1 file MỚI shared/mock/seed/case-state-resolver.js: 5-field state model (APPLICATION/UNDERWRITING_STATUS/UNDERWRITING_DECISION/POLICY_LIFECYCLE + PAYMENT). BANCA.caseStates(app) derive 5 trường (ưu tiên field tường minh, fallback legacy status). BANCA.deriveCaseViewState(app) CANONICAL resolver theo priority §III (cancelled/declined/NMI/uw-pending/condition-unconfirmed/method-required/pending/failed/processing/issuing/issued) → {phase,displayStatus,statusTone,primaryAction,secondaryActions,paymentAccessible,canCreatePaymentIntent,workQueueType,...}. caseStatusBadge(app). Đăng ký head-loader sau product-schemas.
- R2 underwritingDefinition per-product (decisionSource/ruleSetCode/ruleVersion/manualUnit/queueCode): Motor=MOTOR_UW v2.1 manualUnit "Motor Underwriting" queue MOTOR_UW_QUEUE; PA=PA_BASIC_UW v1.0 manualUnit null queue null. underwritingDefinitionFor() config-error khi thiếu (KHÔNG fallback Motor). makeStpDecision record đầy đủ (underwritingDecisionId/decisionSource/ruleSetCode/ruleVersion/additionalPremium/paymentAllowed...).
- R3 submitApp STP set 5-field states + config-error guard.
- R4 REFACTOR consumers dùng resolver (AC-08): submitted list cta+badge+todo (deriveCaseViewState/caseStatusBadge), workspace header badge (caseStatusBadge), caseNextAction, getSubmittedCaseActions (primary+secondary từ resolver), payment tab gating (canCreatePaymentIntent/paymentAccessible), manual UW wsCard unit/queue từ underwritingDefinition (bỏ hard-code Motor).
- R5 payment modal 3 nhóm (QR/CUSTOMER_PRESENT · link+SMS/Email/Copy CUSTOMER_REMOTE · seller SELLER_ASSISTED); createPaymentIntent chống double (AC-13) + resolver guard (AC-09); settlePayment SUCCESS→policyStatus ISSUING→ISSUED (payment giữ SUCCESS); retryIssue (issue-failed giữ SUCCESS không thu lại — AC-10) + policy tab issue-failed card.
- R6 mock 106 (APPROVED_WITH_CONDITION confirmed→METHOD_REQUIRED) / 107 (APPROVED, QR intent PENDING) / 108 (SUCCESS + POLICY_ISSUE_FAILED); giữ 104 NMI + 105 condition-pending regression.
- R7 scripts/test-post-submit.js 16 test PASS (15 spec + AC-07) + test-foundation 58 PASS. Browser walkthrough PASS: PA submit→APPROVED_STP(PA_BASIC_UW)→METHOD_REQUIRED(payment null)→intent QR PENDING(dup guard)→settle SUCCESS→ISSUED JB-PA-2026-7870. Resolver đồng nhất 104(locked)/105(locked)/106(method_required)/107(pending)/108(issue_failed). List badge=CTA khớp resolver. 108 retryIssue giữ payment SUCCESS→ISSUED. Console 0 error. Cache V=v=20260723n.

## Update 13:52 — FIX BUG user báo: "không chọn gói để tính phí được" (draft mới)
- Root cause: draft MỚI (DRAFT-2026-NEW) được DỰNG LẠI từ ctx literal mỗi lần load — mọi patchApp (chọn gói/quote/riskAnswers) ghi vào overlay nhưng KHÔNG merge lại → chọn gói xong reload là mất.
- Fix: (1) sau khi build app literal cho draft mới → Object.assign(app, overlay.applications[app.id]) merge overlay đã patch; (2) khi tạo PHIÊN BÁN MỚI (ctx mới từ params) → delete overlay DRAFT-2026-NEW cũ (tránh dính dữ liệu phiên trước; key overlay=bancaDemoOverlay).
- Smoke: new=1&product=pa → chọn PA_STD → badge "Đã chọn" + quote hiện + qua bước Khai báo rủi ro vẫn giữ PA_STD, câu hỏi PA đúng (hazardous/disability/illness), console sạch. 16+58 test PASS. Cache v=20260723o.

## Update 14:28–15:32 — 3 task lớn Banca Sales Portal (project: sales-service-prototype/prototype/sprint1)

### Bối cảnh kỹ thuật quan trọng (LẶP LẠI nhiều lần)
- Prototype HTML/CSS/JS thuần, mock localStorage overlay (key `bancaDemoOverlay`), seed trong `shared/mock/seed/*.js`, nạp qua `shared/js/head-loader.js` (danh sách file + cache-bust `const V='v=YYYYMMDDx'`).
- **CACHE TRAP**: head-loader nạp seed bằng `<script src=...?v=V>`. Codex sửa file nhưng KHÔNG bump V → browser giữ bản cũ. Khi retest: hoặc BUMP V, hoặc chạy `python3 -m http.server <PORT KHÁC>` để né cache. Đã dùng port 8891/8893/8895/8896/8897 lần lượt.
- **RENDER**: policy detail = cockpit sections (không tab thật); application-workspace = wizard step qua `?step=`. Persona mặc định RM-01 (Nguyễn Văn An, Retail RM, ACTIVE). Customers CUS-001..010 có `dob`. PA policy seed: `JB-PA-2026-1201` (productType:'pa'). Motor policies JB-POL-*.

### Task A — PA Overhaul (Codex + tự retest) → PASS 12/12 AC
- Giao Codex qua BRIEF-PA-OVERHAUL.md. Sửa ~16 file. Report: PA-OVERHAUL-REPORT.md.
- Kết quả verify browser: PA insured party tính tuổi từ DOB+effective read-only (41), package đủ 6 nhóm quyền lợi + So sánh, risk declaration dynamic branch, PA Policy Detail renderer riêng (0 field Motor, còn 361 ngày tính động), payment UX 2 bước (experience→config, intent chỉ tạo sau bước 2, paymentId≠merchantReference≠gatewayTransactionId), success KHÔNG alert (toast+inline+auto-nav), policy persist bằng policyId (JB-MT-2026-7644), routing ?view=detail&id=policyId + row click + refresh OK, not-found state OK.

### Task B — Chuẩn hóa thuật ngữ (Codex + tự retest + TỰ VÁ REGRESSION)
- Giao Codex qua BRIEF-TERMINOLOGY.md. Tạo `shared/js/terminology.js` (BANCA.T + BANCA.t()), `scripts/validate-terminology.js` (chạy `node scripts/validate-terminology.js` → PASS 79 files). Report: TERMINOLOGY-REPORT.md.
- **⚠️ BÀI HỌC LỚN**: Codex thay chuỗi "seller"→"nhân viên tư vấn" LỌT VÀO CODE → 2 SyntaxError làm TRẮNG hàng loạt trang:
  1. `shared/mock/seed/manager-profiles.js`: param `function(referrer,advisor,seller)` bị đổi thành `nhân viên tư vấn` → cả file không nạp → `BANCA.customerCell` undefined → trang danh sách trắng.
  2. `modules/team-workspace/index.html`: object key `seller:`→`nhân viên tư vấn:`, property `e.seller`, query key `?view=seller&seller=`, `qs.get('seller')`, CSS `.seller-2col` đều bị thay.
- Cách phát hiện: `node --check` từng .js + parse mọi inline `<script>` bằng `new Function()`. Cách vá: revert token kỹ thuật về `seller` (enum 'Support Nhân viên tư vấn' giữ vì đổi NHẤT QUÁN seed+so sánh). Thêm `help:'Trợ giúp'` vào terminology.js (sidebar trước hiện 'HELP'). Bump V→20260723q.
- Verify browser: unsubmitted/submitted/team-workspace render OK, glossary đúng (Yêu cầu bảo hiểm chưa/đã nộp, Mã yêu cầu, Nhân viên phụ trách, Tạo yêu cầu bảo hiểm, Tiếp tục yêu cầu gần nhất), 0 từ cấm.
- **QUY TRÌNH CHỐT cho mọi task giao Codex sau này**: brief PHẢI cấm replace token trong code; sau Codex PHẢI tự chạy node --check + parse inline + validate-terminology TRƯỚC khi retest browser.

### Xung đột thuật ngữ CHƯA CHỐT (chờ user)
- Tab risk-object của xe: task đầu user bảo đổi "Xe"→"Đối tượng bán" (đã làm ở journey-registry.js:123); glossary chuẩn hóa dùng "Đối tượng bảo hiểm". Hiện LỆCH nhau. Đã hỏi user 2 lần (bị cancel). Chờ user chốt 1 tên.

### Task C — Quản lý tài liệu Policy Detail (ĐANG CHẠY nền lúc flush)
- Giao Codex qua BRIEF-DOC-MGMT.md (session clear-crustacean). Yêu cầu: action bar gộp còn "Tài liệu & gửi khách", section tài liệu chọn được (checkbox + permission canView/canDownload/canSendToCustomer), tải 1 file/nhiều→ZIP, modal gửi khách + validation, audit+timeline, thuật ngữ display-only, responsive. 14 AC. Anchors: policies/index.html actionBar~L162, docLinks Motor~L175, PA docs~L39. Report sẽ là DOC-MGMT-REPORT.md.
- TODO còn lại khi flush: chờ Codex xong → verify syntax → retest browser 15 bước + screenshot + rà 14 AC + báo cáo.

## 2026-07-23 19:57 — Durable session notes before compaction

### Confirm-pay redesign (Submitted Case Workspace)
- User requested redesign of tab **"Xác nhận & thanh toán"** for Motor + Health.
- Final decision: use **OpenClaw/Claude, not Codex**. First Claude run `delta-shore` hit session limit; user chose **"Chờ Claude"**; second run `quiet-gulf` succeeded after reset.
- Commit completed: `a5ff493 Submitted workspace: redesign Xác nhận & thanh toán tab with strict confirmation/payment gating`.
- Report written: `projects/sales-service-prototype/prototype/sprint1/CONFIRM-PAY-REPORT.md`.
- Implemented: removed confirmpay subtabs including **Liên hệ**; one vertical page with 6 sections: process status, customer confirmation, fee payable, direct payment methods, current payment status, payment history.
- Implemented strict gating in `shared/mock/seed/case-state-resolver.js`: payment only when underwriting approved + required confirmation complete + required docs complete + quote valid + amount > 0 + no blocker; Health requires all active members confirmed. Seller cannot self-mark SUCCESS; only gateway/demo callback changes payment success. Policy issuance/banner gated by valid payment + `policyStatus === ISSUED`.
- Implemented direct 3 payment cards: QR at counter, remote payment request, payment on this device; removed “Chọn cách thanh toán”. Technical English fields are hidden in “Chi tiết kỹ thuật”. Payment history is table + row detail, not repeated key-value list.
- Migrated invalid demo state combos in seed data; report says seed-scan found 0 invalid combos. Added Health retest apps `APP-2026-HLT3` (member confirmation incomplete; payment locked) and `APP-2026-HLT4` (all confirmed; payment available).
- Tests after Claude + manual verify: `node --check` JS PASS, inline scripts PASS, `validate-terminology` PASS 80 files, `test-post-submit` PASS 16/16, `test-foundation` PASS 58/58. Browser evaluate checked Motor `APP-2026-106`, Health incomplete `APP-2026-HLT3`, Health eligible `APP-2026-HLT4`.
- Cache bust after confirm-pay: `shared/js/head-loader.js` `const V = 'v=20260723w';`.

### Terminology decision
- User explicitly chose **"Đối tượng bảo hiểm"** over **"Đối tượng bán"**.
- `shared/mock/seed/journey-registry.js` now uses `{id:'RISK_OBJECT', label:'Đối tượng bảo hiểm', component:'motorVehicle'}` and glossary label `RISK_OBJECT: {label:'Đối tượng bảo hiểm'}`.
- Search showed no remaining `Đối tượng bán` in `modules`/`shared` at that time. Syntax OK.

### Issued policy action consolidation
- User requested hiding duplicate buttons on issued-policy screen: hide Policy Detail / Tải PDF / Gửi lại khách, hide Xem lịch sử action, make Xem hợp đồng a hyperlink to policy detail, add Tải hợp đồng + Gửi cho khách in header, hide duplicate bottom download/send.
- Commit: `1aecc03 Issued policy view: consolidate actions — 'Xem hợp đồng' hyperlink to detail + Tải hợp đồng/Gửi cho khách in header; hide duplicate button groups (Mở Policy Detail/Tải PDF/Gửi lại khách + panel download/send) + Xem lịch sử`.
- Advisor review follow-up verified `getSubmittedCaseActions` has one call-site in app-workspace header, guard `st==='ISSUED' && app.policyId`, and Health issued app `APP-2026-HLT1` links to `JB-HEALTH-2026-2201`.
- Open UX question remains: button **"Gửi cho khách"** on issued policy header currently stays 1-click per earlier user ask; advisor noted possible UX conflict with document center modal. User has not chosen modal vs 1-click yet.

### Advisor / process lessons
- Advisor flagged major risk: many agent changes without git commits. Resolved by checkpoint commits: `3e17783` broad PA/terminology/doc-mgmt/health journey/multi-insured changes, `94c184f` defensive RISK_OBJECT guard, `1aecc03`, `a5ff493`.
- Continue rule: commit after each agent task passes; use path-specific commits when an agent is running to avoid sweeping WIP.

### Current user-reported issues at end of session (not yet implemented)
- User reported **layout bug** via screenshot in Policy Detail: right rail overlaps/truncates main content; likely `.policy-cockpit .pc-grid` / `.pc-main` needs `min-width:0` and overflow containment across duplicated policy detail CSS blocks in `modules/policies/index.html`.
- User requested: in Health member form, **Thông tin người đại diện** should allow selecting existing members in the hồ sơ or adding a new representative.
- User requested: sticky action buttons should be a **common system rule** across the prototype when scrolling.
- A plan was started just before this memory flush to patch: Policy Detail grid, Health representative select/add-new, and global sticky action bar. Plan exists at `PLAN-91218864.md`, but no todos/execution completed yet for that patch.

## 2026-07-23 — OCR người được BH (Health)

Thêm tính năng OCR quét CCCD/khai sinh ở **tab Người được bảo hiểm** của SP Health (bước INSURED_PARTY, healthInsuredPerson), mirror "moto rule" của tab tài liệu xe.

**File:** modules/application-workspace/app-workspace.js
- `_healthPersistMembers`: thêm field `ocr` để persist marker theo member.
- Thêm `window.healthUnitOcr(id,unitId,inputEl)` + helper `_ocrDobToISO`: đọc ảnh → saveCapturedDoc (vào mục Tài liệu) → BANCA.mockOcr(NATIONAL_ID) → prefill name/dob(ISO)/gender/identityNumber cho đúng member → reload giữ unit.
- Member card: thêm block "Quét giấy tờ / Chụp ảnh" + box tiến trình #ocr-hm-<uid> + panel "Đã bóc tách · tin cậy · badge NOT_REVIEWED" + note "OCR chỉ prefill". Ẩn khi member inactive / readOnly.

Node --check OK. Mỗi member 1 nút quét độc lập (multi-insured family).

## 2026-07-23 — Thêm demo data Health

Bổ sung mock cho SP Health (applications.js, policies.js):
- Chưa nộp: DRAFT-2026-HLT2 (cá nhân, bước Khai báo), DRAFT-2026-HLT3 (đôi vợ/chồng, bước Tài liệu, thiếu bản khai).
- Đã nộp: APP-2026-HLT5 (PENDING_PAYMENT), APP-2026-HLT6 (NEED_MORE_INFO - bổ sung bệnh án).
- Hợp đồng: APP-2026-HLT7 (ISSUED) + policy JB-HEALTH-2026-2202 (Trịnh Mỹ Linh + con).
node --check OK cả 2 file.
