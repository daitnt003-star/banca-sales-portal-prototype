# 2026-07-27 — Banca Sales Portal Rework v2

## Bối cảnh dự án
- **Base code:** `projects/sales-service-prototype/prototype/sprint1` (Spec v1, manifest v0.2.0, 487 files, 11 module). KHÔNG rebuild — chỉ patch surgically, tái sử dụng journey engine + design tokens đã có.
- **Local preview:** `python3 -m http.server 8123` chạy trong thư mục sprint1 → http://localhost:8123/index.html. Log `/tmp/banca-http.log`.
- **Path thực tế:** workspace nằm ở `/Users/trixie/aicoworker/openclaw/workspace-bancassurance/` (không phải `openclaw/` như runtime ghi).
- **Cache-bust:** `shared/js/head-loader.js` biến `V='v=YYYYMMDDx'`. Mỗi lần sửa shared file phải bump (đang ở `v=20260727i`). User cần hard refresh ⌘⇧R.
- **Verify workflow đã chốt:** node --check syntax + node logic test (shim `window/localStorage/document/location`) → browser tool thật (console errors level=error + screenshot). Browser targetId hiện tại: F03C08C2B0647BA5EC639917E8D84535.
- **Persona/Channel switch:** localStorage `bancaPersona` + `bancaChannel`. Demo browser đang set persona RM-01.

## Kiến trúc đã dựng (config-driven, KHÔNG clone theo channel/product)
Files foundation mới trong sprint1:
- `shared/mock/seed/channel-profiles.js` — 3 ChannelProfile (BANCA_INTEGRATED/STANDALONE/AGENT_BROKER) + `BANCA.channel()/setChannel()/channelProfile()`.
- `shared/mock/seed/customer-data-access.js` — 4 stage ANONYMOUS→CONSENT→IDENTIFIED→VERIFIED + `DATA_SHARING_GRANT` (GRANTED_BY_SOURCE/CUSTOMER) + `fetchCustomerPII()` + `stageFromGrant()`.
- `shared/mock/seed/status-mappings.js` — 16 quote status → 5 nhóm + `paymentEnableRule()`.
- `shared/mock/seed/quote-version.js` — versioning + reRate→SUPERSEDED, cấm sửa phí tay.
- `shared/mock/seed/offer-filters.js` — DATA_SCOPES + OFFER_GROUP_FILTERS (mỗi lifecycle 1 bộ quick-filter riêng) + `applyGroupFilters()` (match statuses/phases/sla) + `applyUnitFilter()` + `isSlaSoon()`.
- `shared/js/navigation-config.js` — nav Bản chào/Hợp đồng.
- `shared/components/foundation-components.js` — CustomerContextCard, DataSourceBadge, ConsentStatus, ChannelSwitcher, **dataScopeSelector (dropdown)**, offerGroupBar, offerQuickFilters, customerTaskCell, offerCta, fmtDateTime, money, ensureIdentifiedPrefill, appDataAccessStage.
- `shared/components/sales-context-offer.js` — SalesSourceBar + OfferSelectionWorkspace (PACKAGE_CATALOG/ADDON_CATALOG mock, package/addon/compare/alternatives + handlers selectPackage/toggleAddon/switchProduct persist qua BANCA.patchApp).
- Tất cả đăng ký trong `head-loader.js`. CSS trong `shared/styles/components.css`.

## Correction quan trọng từ user (đã áp)
1. **Consent Banca:** happy path Banca đã có grant từ NGÂN HÀNG (GRANTED_BY_SOURCE) → IDENTIFIED ngay, tự prefill PII (thao tác hệ thống, không reload), **KHÔNG có nút seller bấm "Khách đồng ý chia sẻ dữ liệu"**. Anonymous+consent gate CHỈ cho Quick Advice chưa định danh. HANDOVER kế thừa stage phiên nguồn.
2. **Gộp Khách hàng + Ngữ cảnh + Phương án → 1 stage "Khách hàng & phương án"** (= CUSTOMER_INFO relabel, prepend SalesSourceBar + append OfferSelectionWorkspace). Bỏ tab Khách hàng riêng, bỏ step OFFER_CONTEXT tách rời (branch dead-code còn trong app-workspace.js). **Banca Integrated bấm "Tạo YCBH" → KHÔNG popup**, đi thẳng stage (openStartSale channel-aware trong app-shell.js).
3. **Nav §8.1:** "Bản chào" là 1 object (offerGroupBar 5 nhóm nối unsubmitted↔submitted qua param `g5`), bỏ framing "chưa nộp/đã nộp". Khi có g5, ẩn dãy chip cũ (single source of truth). g5→APP_STATUS map trong `BANCA.OFFER_G5_MAP`.
4. **DataScopeSelector dropdown** thay 4 tab; per-lifecycle quick filters; WAIT_CUST filters: Tất cả/Chờ xác nhận/Chờ OTP/Chờ thanh toán/Thanh toán thất bại/Sắp hết hạn.
5. **Bảng task-oriented:** gộp Việc-cần-làm+Trạng thái+KQ-thẩm-định → 1 cột "Khách cần thực hiện"; scope Của tôi ẩn cột NV phụ trách; CTA từ nextAction (1 primary/dòng, pin phải); **KHÔNG cho seller xác nhận thay khách** → "Gửi yêu cầu xác nhận"/"Theo dõi xác nhận"/"Gửi lại thanh toán"; ngày dd/MM/yyyy·HH:mm, tiền x.xxx.xxx ₫ nowrap; bỏ control debug.

## Deliverables docs
`sprint1/docs/rework-v2/` : A-impact-analysis.md, B-component-reuse-matrix.md, C-state-transition-map.md, D-source-of-truth-index.md.

## Trạng thái & lộ trình (§22)
- ✅ Phase 0 (deliverables) · Phase 1 (foundation) · Phase 2 (entry/consent) · Phase 3 (stage Khách hàng & phương án) · các correction nav/scope/table.
- ⏳ **CÒN LẠI:** Phase 5 (Xác nhận & Thanh toán dùng chung ConfirmationPaymentPanel + OtpVerificationPanel dual-mode + underwriting) ← BƯỚC TIẾP THEO; Phase 6 (Policy Cockpit 6 tab); Phase 7 (Dashboard/lists + Manager Workspace my/team split); Phase 8 (self-service + regression).
- Polish còn nợ nhẹ: nav rename mới áp submitted/unsubmitted; unsubmitted (PREPARING) header vẫn dùng uxListHeader cũ (chưa compact như submitted).

## Commits chính (branch mặc định, repo sprint1/.git)
7fd677b P0+1 foundation · 5baa2ca P2 consent · 6a8d728 correction consent+OFFER_CONTEXT · 57f2c6e channel in state-gallery · 52f05e1 channel in header tool · 4f22a66 nav Bản chào + 5-group · 6dbfc89 fix duplicate filter bars · 0cd809c config-driven filters+DataScopeSelector · ea753a3 task-oriented table redesign.

## Cách làm việc user ưa thích (quan trọng)
- Làm **phase-by-phase, mỗi phase xong thì tóm tắt** (dùng show_adaptive_ui report). Luôn hỏi trước khi qua phase mới.
- User gửi correction rất chi tiết kèm AC → làm theo AC, verify từng AC bằng browser thật + screenshot.
- Dùng ask_user_question để xác nhận plan trước refactor lớn.
- Ưu tiên KHÔNG phá seed demo cũ (opt-in flags, guard theo isNew/channel).

---

## Skill mới: `banca-orchestrator` (agent điều phối)

**Vị trí:** `~/.codex/skills/banca-orchestrator/` (runtime Codex/OpenClaw — user chọn)
**Gọi bằng:** `/banca-orchestrator "<yêu cầu>"`
**SkillSpector:** 20/100 LOW/SAFE, không có executable script.

### Quyết định thiết kế (user chốt qua AskUserQuestion)
1. Runtime: **chỉ Codex/OpenClaw** (không symlink sang ~/.claude/skills)
2. Confirm gate: **1 gate duy nhất** — duyệt kế hoạch rồi chạy hết tới khi QC pass (không gate mỗi phase; vẫn tóm tắt sau mỗi phase nhưng không dừng)
3. Subagent: **file role riêng**. Canonical ở `banca-orchestrator/agents/*.md`; bản mỏng cho Claude Code ở `.claude/agents/banca-{ba,designer,frontend,qc}.md` trỏ về canonical (không nhân bản nội dung)
4. Phạm vi: **chuyên Bancassurance** — nhúng sẵn state model, thuật ngữ, persona, ranh giới 3 tầng

### Luồng 6 phase
PHASE 0 Intake (phân loại T1–T6) → PHASE 1 Hiểu vấn đề (5 lăng kính: brainstorm/research/analyst/problem-solving/phản biện) → **PHASE 2 CONFIRM GATE** (bảng định tuyến + skill bị loại + giả định + rủi ro, chờ duyệt) → PHASE 3 Thực thi BA→Designer→Dev FE → PHASE 4 QC (tối đa 2 vòng sửa) → PHASE 5 Báo cáo + ghi memory.

T6 (hỏi đáp thuần) là loại DUY NHẤT được bỏ qua gate.

### Phát hiện khi research
- 20 skill hiện có nằm ở `~/.codex/skills/`, KHÔNG phải `~/.claude/skills/` (Claude Code không thấy chúng)
- 3 skill lõi (requirement-ba / enterprise-uiux / portal-prototype-builder) ĐÃ có sẵn chuỗi handoff `BA → UI/UX → Structure → Frontend → Review` trong `references/handoff-contracts.md` — orchestrator bám đúng chuỗi này
- **Khoảng trống:** không có skill cài sẵn cho research/analyst/problem-solving/critical-thinking/QC → xử lý bằng `references/thinking-protocol.md` và `agents/qc.md`
- "Structure Skill" và "Review Skill" được 3 skill lõi tham chiếu nhưng KHÔNG tồn tại; portal-prototype-builder gánh phần Structure, QC agent gánh phần Review

---

## Nâng năng lực UI/UX cho AIcoworker — GP1–GP5 (2026-07-27, chiều)

**Vấn đề user nêu:** AIcoworker tư duy UI/UX kém.
**Chẩn đoán (có số đo, không phải cảm tính):** không phải model kém thẩm mỹ — **thiếu ràng buộc**.

### 4 nguyên nhân gốc đo được
1. **Không có design system, chỉ có bảng màu.** tokens.css có 28 biến, 100% là màu+font. Zero thang spacing/radius/shadow/type/z/motion. Hệ quả: 139 mã hex hardcode (479 lần dùng), **19 cỡ chữ khác nhau gồm 12.5px/11.5px/13.5px/10.5px/16.5px**. Cỡ chữ nửa pixel = dấu vân tay của giá trị được SINH RA thay vì được CHỌN.
2. **Không skill nào sở hữu thẩm mỹ.** `enterprise-uiux-skill` rule #26 "Do not write implementation code" + #27 "Do not specify exact CSS" → cố ý từ chối. `requirement-ba-skill` khai không sở hữu visual. `portal-prototype-builder` viết code nhưng nhận spec không có số. → Thẩm mỹ rơi vào khoảng trống giữa 3 skill.
3. **Agent không bao giờ NHÌN thấy output.** 5 validator cũ toàn structural. globals.css=27 bytes, layout.css/components.css ghi "kept in tokens.css" → phân lớp chỉ là hình thức, tất cả dồn vào 1 file 869 dòng.
4. **Khuyến nghị UX không cưỡng chế được.** `enterprise-uiux-skill` ĐÃ chẩn đoán đúng "pattern drift" trong docs/ux/reviews/cross-screen-layout-review-2026-07-21.md nhưng chỉ là markdown, không chặn được batch patch sau.

### Đã làm
- **GP1** `shared/styles/tokens.css` — thêm 8 nhóm thang, **rút từ histogram usage thật** (không import generic để giữ mật độ compact đã nghiệm thu): space 12 bậc (0–48px, cơ sở 2px) · text 7 bậc (11–24px) · leading 4 · weight 4 · radius 5 · shadow 3+focus · border 2 · z 6 tầng · duration 3 · breakpoint 4. Additive, không đổi biến cũ.
- **GP3** `scripts/validate-design-tokens.js` — **đọc thang TỪ tokens.css**, không hardcode → sửa thang 1 chỗ validator theo ngay. 11 luật, phân error/warn. Có `--strict` (fail build), `--rule`, `--top`. Bỏ qua nội dung trong `<code>`/`<pre>` (ví dụ tài liệu).
  `scripts/fix-design-tokens.js` — codemod, **dry-run mặc định**, có `--max-delta` làm van an toàn, tự tạo .bak.
- **GP4** `dev/design-reference.html` — render trực tiếp tokens.css bằng getComputedStyle, KHÔNG chép giá trị → luôn khớp. Là nơi agent mở trước khi viết CSS.
- **GP2** skill mới `~/.codex/skills/visual-design-system/` (SkillSpector **0/100 SAFE**) — sở hữu đúng thứ enterprise-uiux-skill từ chối. 5 mode A–E, 20 mandatory rule, 3 references + 2 templates.
- **GP5** vá `shared/templates/insurance-prototype-ui-template.html` (nguồn lây lỗ hổng sang mọi dự án sau) + guide.

### Kết quả đo
| | Trước | Sau |
|---|---|---|
| Vi phạm thang | 2.078 lỗi | **1.191 lỗi** |
| Cỡ chữ nửa pixel | 414 | **5** (đều lệch >1px nên codemod cố ý không đụng) |
| Đã nắn | — | 887 giá trị / 17 file, toàn bộ lệch ≤1px |

Còn tồn: 636 OFF_SCALE_SPACING, 347 HARDCODED_COLOR, 88 OFF_SCALE_FONT_SIZE, 61 radius, 30 shadow, 24 z-index — đều là loại lệch ≥2px hoặc cần quyết định ngữ nghĩa (màu/shadow/z không tự động nắn được).

### Chuỗi orchestrator đã đổi
`BA → Designer (cấu trúc) → **Visual (con số)** → Dev FE → QC`
Thêm loại yêu cầu **T7 — chất lượng thị giác**. QC nay bắt buộc chạy `validate-design-tokens.js --strict` và **phải chụp màn hình đối chiếu design-reference trước khi kết luận PASS**.
Role file: `banca-orchestrator/agents/visual.md` + `.claude/agents/banca-visual.md`.

### ⚠️ CHƯA LÀM — cần user xác nhận
**Chưa xem UI bằng mắt sau khi nắn 887 giá trị.** Đã verify: ngoặc CSS cân bằng (484/484), 3 file JS lớn parse sạch, template gốc cân bằng. NHƯNG chưa mở browser. Máy không có playwright/puppeteer (có Chrome app).
→ **Việc tiếp theo: mở prototype kiểm tra bằng mắt.** Nếu vỡ, khôi phục bằng file .bak (đã thêm *.bak vào .gitignore).

### Nguyên tắc rút ra (áp cho mọi dự án sau)
**Ràng buộc thắng khiếu thẩm mỹ.** Agent buộc chọn từ thang 12 bậc ra kết quả nhất quán hơn agent được tự do chọn số — kể cả khi agent thứ hai "biết" thẩm mỹ hơn. Và: **prompt thì agent quên, script thì không** — chuẩn không có validator sẽ bị phá bởi chính batch patch tiếp theo.
