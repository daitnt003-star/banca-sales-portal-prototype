
## 2026-07-24 — Đồng bộ OCR Health = component chung với Motor

User: giao diện/tính năng OCR tài liệu của Health phải DÙNG CHUNG component với Motor (Motor là chuẩn), không phải mỗi đối tượng 1 style.

Refactor app-workspace.js:
- BỎ block OCR custom + `window.healthUnitOcr` (dashed box, nút "Quét giấy tờ/Chụp ảnh", alert panel riêng).
- Mỗi member INSURED_PARTY nay render bằng **BANCA.docItemHtml(code:"HID-<uid>", docType:NATIONAL_ID, ocr:enabled)** — giống hệt doc-item xe (Tải lên/📷/Thay thế/Xem + badges + preview modal). Upload đi qua window.docUpload dùng chung.
- Thêm `window.healthSyncOcrToMembers(id)` + đặt `window.__docRefresh` trong nhánh render Health: sau upload → đồng bộ extracted (fullName/dob ISO/gender/idNumber) vào đúng member (cờ appliedToMember, không ghi đè sửa tay) → reload, y hệt Motor reload sau upload.
- readOnly → ocr:none, locked:true (chỉ Xem).
node --check OK, không còn ref cũ.

## 2026-07-24 — UI báo giá Motor = style Health

User: làm lại UI báo giá Motor tương tự Health (ảnh: card BÁO GIÁ GIA ĐÌNH — dòng flex gạch đứt → tổng đậm brand → caption).

app-workspace.js (nhánh PACKAGE_AND_QUOTE motor):
- BỎ bảng <table> waterfall (wfRow) → thay bằng helper qRow() dòng flex y hệt famBlock Health: label trái (ink-500), số phải đậm, teal=giảm, amber=cộng, gạch đứt từng dòng.
- Tổng "Phí phải đóng" = dòng đậm 15px màu brand-600 trên border-top solid (giống "Tổng phí gia đình").
- Caption xám + nút (Tính phí lại / Khôi phục / Lịch sử) xuống hàng dưới, layout 1 cột full-width.
- GIỮ nguyên id JS: quote-block, q-badge, q-table-wrap, q-total, q-total-label, q-dirty-note, rate-btn → markStale/autoRerate/rerate vẫn chạy.
- Bump index.html ?v=20260724a.
Test live (http.server 8899, DRAFT-2026-010, persona TS-01): render đúng style Health, toggle add-on → autoRerate → tổng cập nhật 8.031.100, V2. OK.

## 2026-07-24 — Đồng nhất step "Đối tượng bảo hiểm" + Motor risk-object theo format Health

1) RENAME step label thống nhất mọi sản phẩm → "Đối tượng bảo hiểm":
   - journey-registry.js: STAGE_CATALOG.INSURED_PARTY, PA stage, Health stage (đều "Người được bảo hiểm" → "Đối tượng bảo hiểm"). RISK_OBJECT giữ "Đối tượng bảo hiểm".
   - status-model.js: INSURED_PARTY + RISK_OBJECT → "Đối tượng bảo hiểm".
   - Stepper đọc từ BANCA.journeyEditStages → Health/PA giờ hiện "Đối tượng bảo hiểm" như Motor.

2) RESTYLE Motor RISK_OBJECT (motorVehicle) theo format Health:
   - BỎ inputMethods (nút Quét/Chụp/Nhập tay riêng) + extractedPanel (lưới OCR riêng) + OCR_SHOW + g1/g2.
   - Thay bằng: infoBanner (alert2 info) + section-title "Đối tượng bảo hiểm" + 1 card đối tượng (border-left brand, header "🚗 <biển số · hãng dòng> [badge OCR]") chứa doc-item OCR DÙNG CHUNG (BANCA.docItemHtml regDef) + label + lưới trường nhận dạng/kỹ thuật. conflictCard → alert2 warn inline. g3 (định giá) + g4 (thế chấp) bỏ số thứ tự, giữ nguyên logic.

CACHE: seed load qua head-loader.js với V nội bộ + index.html ?v. Phải bump CẢ HAI: head-loader V→20260724b VÀ head-loader.js?v=43→44 ở 11 file index.html (nếu không, browser cache head-loader cũ → seed cũ). app-workspace ?v→20260724b.

Test live: Motor DRAFT-010 RISK_OBJECT render đúng format Health (doc-item Tải lên/📷). Health DRAFT-HLT2 stepper chip = "Đối tượng bảo hiểm". OK.

## 2026-07-24 — Card khách hàng (CUSTOMER_INFO) theo chuẩn card Health

User: lấy card thông tin của Health làm chuẩn, cập nhật card khách hàng Motor.

CUSTOMER_INFO là branch DÙNG CHUNG mọi sản phẩm (không product-specific) → sửa 1 chỗ, đồng bộ cả Motor + Health + PA.
- BỎ modeHead (card nguồn tách rời) + lưới trường trần trên nền.
- GỘP thành 1 card kiểu member-card Health: border-left brand (bankFed) / amber (prospect); header = tên KH + badge CIF/Prospect + nút "Xem ngữ cảnh nguồn"; dòng nguồn (srcLine); doc-item OCR có label (dùng chung docItemHtml); lưới trường; banner KYC — TẤT CẢ trong 1 card.
- Prospect: header "Khách hàng mới [Cần định danh]" viền amber, consent + doc-item + form cf-*.
- app-workspace ?v→20260724c.
Test live DRAFT-2026-RN1 (Lý Thu Trang, bankFed): render đúng chuẩn card Health, mọi trường trong 1 card viền trái brand. OK.

## 2026-07-24 — Tab Tài liệu Motor: layout giống Health + select-to-add cho tài liệu khác

User: đồng bộ layout tab Tài liệu Motor giống Health; chỉ hiển thị tài liệu bắt buộc; tài liệu khác cho select chọn thêm, không show hết.

app-workspace.js (nhánh DOCUMENTS motor - else branch):
- Thêm info banner (alert2 info) đầu tab như Health.
- legendInner: BỎ hàng legend ●◐○, chỉ giữ counter "Tài liệu bắt buộc: x/y" (id doc-legend cho __docRefresh vẫn nguyên).
- Section "Tài liệu bắt buộc" giữ nguyên (reqRender doc-item).
- THAY section "Tài liệu khác" (trước liệt kê hết otherRender) → "Tài liệu khác (tùy chọn)": chỉ hiện doc đã tải HOẶC user chủ động thêm. Có <select id=add-other-doc> + nút "Thêm tài liệu"; doc đã thêm hiện doc-item + nút "Bỏ khỏi danh sách".
- window.addOtherDoc/removeOtherDoc lưu localStorage banca_otherdocs_<appid>, reload.
- app-workspace ?v→20260724d.
Test live DRAFT-2026-RN1: chỉ hiện 1 doc bắt buộc (Giấy đăng kiểm) + dropdown. Chọn "Giấy đăng ký xe" → thêm thành row có Tải lên + Bỏ khỏi danh sách; REG rời khỏi dropdown. OK.

## 2026-07-24 — TÓM TẮT PHIÊN (unification Motor↔Health) + bài học chốt

**Chủ đề xuyên suốt:** User yêu cầu đồng nhất UI/component giữa các sản phẩm, lấy Health làm chuẩn cho nhiều thứ. Nguyên tắc: "component gọi ra dùng cho nhiều đối tượng, không mỗi sản phẩm 1 style".

**Đã hoàn thành trong phiên (đều test live OK, screenshot xác nhận):**
1. OCR người được BH Health → dùng chung BANCA.docItemHtml + window.docUpload như Motor (bỏ healthUnitOcr custom). Sync qua window.healthSyncOcrToMembers + __docRefresh.
2. UI báo giá Motor → style card Health (qRow flex, tổng đậm brand). Giữ id JS: quote-block,q-badge,q-table-wrap,q-total,q-dirty-note,rate-btn.
3. Step "Người được bảo hiểm" → RENAME thành "Đối tượng bảo hiểm" cho MỌI sản phẩm (journey-registry.js catalog+PA+Health, status-model.js). RISK_OBJECT đã sẵn tên này.
4. Motor RISK_OBJECT (tab Đối tượng bảo hiểm) → format Health: infoBanner + section-title + 1 card đối tượng (doc-item OCR dùng chung + lưới trường). Bỏ inputMethods/extractedPanel/g1/g2.
5. Card khách hàng (CUSTOMER_INFO) → chuẩn card Health: 1 card viền trái brand, header tên+badge+nút, doc-item OCR có label, lưới trường, banner — DÙNG CHUNG mọi sản phẩm (Health tự đồng bộ theo).
6. Tab Tài liệu Motor → layout Health: info banner + counter gọn (bỏ legend ●◐○) + chỉ hiện doc BẮT BUỘC + "Tài liệu khác" = select-to-add (addOtherDoc/removeOtherDoc, localStorage banca_otherdocs_<id>).
7. Thêm demo data Health: drafts HLT2/HLT3, submitted HLT5/HLT6, issued HLT7 + policy JB-HEALTH-2026-2202.

**KIẾN TRÚC QUAN TRỌNG:** CUSTOMER_INFO là branch DÙNG CHUNG (không product-specific) → sửa 1 chỗ đồng bộ cả Motor/Health/PA. Health INSURED_PARTY component='healthInsuredPerson', Motor RISK_OBJECT component='motorVehicle' (dispatch qua BANCA.journeyStageComponent). Component OCR chuẩn = BANCA.docItemHtml (ocr-policy.js) + window.docUpload + BANCA.docAutoFill; reload sau upload qua window.__docRefresh (mỗi step tự set).

**⚠️ BÀI HỌC CACHE (rất quan trọng, đã mắc lỗi trong phiên):**
- Prototype ở sales-service-prototype/prototype/sprint1. Seed load qua shared/js/head-loader.js có biến `V='v=YYYYMMDD?'` cache-bust cho TẤT CẢ shared/seed files.
- index.html nạp head-loader.js với `?v=NN` RIÊNG (43→44...) + app-workspace.js với `?v=YYYYMMDD?` RIÊNG.
- Sửa SEED (journey-registry, status-model, ocr-policy, applications, policies...) → PHẢI bump `V` trong head-loader.js VÀ bump `head-loader.js?v=NN` ở 11 file index.html (nếu chỉ bump V, browser dùng head-loader cũ đã cache → seed cũ).
- Sửa app-workspace.js → bump `app-workspace.js?v=` trong index.html của module đó.
- Test local: `python3 -m http.server 8899` tại thư mục sprint1; route `?id=<appid>&step=<STAGE>`; persona set qua `localStorage.bancaPersona` (default RM-01). ACCESS_DENIED nếu app.owner khác persona → đổi bancaPersona.
- Phiên này đã bump: head-loader V=20260724b, head-loader.js?v=44 (11 index.html), app-workspace.js?v=20260724d (mới nhất).

**Motor drafts test:** DRAFT-2026-010 (TS-01), DRAFT-2026-RN1 (RM-01, renewal, Lý Thu Trang), DRAFT-2026-011 (RM-02). Health drafts RM-01: DRAFT-2026-HLT2 (Trịnh Mỹ Linh, bankFed), HLT3 (Lý Thu Trang, đôi).

**LỖI EDIT hay mắc:** khi tạo old_string cho Edit tool, dễ tự gõ sai ký tự tiếng Việt (vd "hoособ", "chг12ấp") → mismatch. Copy chính xác từ Read, chia edit nhỏ.

## 2026-07-24 — Tracking-mode header gọn: stepper lên top + bỏ alert

User (view hồ sơ ĐÃ NỘP - tracking mode, shared mọi sản phẩm): đem section "Trạng thái xử lý" (node stepper) lên header thay card Giai đoạn; meta (Việc tiếp theo/SLA/phiên bản/owner) gọn lại; bỏ hết alert ở từng tab.

app-workspace.js:
- Tách caseStepperStrip() (từ cpProcessSection cũ) = strip 6 node (Chấp thuận→Xác nhận KH→Chờ thanh toán→Đang xử lý→Thanh toán thành công→Phát hành HĐ). BỎ cpProcessSection, bỏ khỏi body confirmpay (line ~2286).
- hdr: thay ${dashStrip} (5 chip card Giai đoạn/Việc tiếp/SLA/PB/Owner) → caseStepperStrip() + dòng meta inline gọn "Việc tiếp theo · SLA · Phiên bản · Owner". Stepper giờ hiện ở MỌI tab (header sticky).
- viewOnlyBanner → ""; bỏ notiBanner khỏi shell concat.
- Bọc toàn bộ content trong <div class=track-clean> + <style>.track-clean .alert2.info{display:none}</style> → ẩn hết alert giải thích màu xanh ở mọi tab (giữ warn/danger actionable + success teal inline).
- Renumber section confirmpay: cpCard 2..6 → 1..5 (vì §1 stepper lên header).
- app-workspace ?v→20260724f.
Test live APP-2026-HLT5 tab confirm + overview: stepper ở top, meta gọn, sạch alert. OK.

## 2026-07-24 — Motor Đối tượng BH: checkbox "Bên mua = người được BH chính" checked+disabled

Thêm vào vehicleCard (RISK_OBJECT motor) label checkbox "Bên mua đồng thời là người được bảo hiểm chính" checked disabled (cố định, xe: bên mua=chủ xe=người được BH) — đồng bộ hình thức với Health. ?v=20260724g. Renumber section confirmpay đã xong ở bản f (cache: user cần hard refresh).

## 2026-07-24 — NGUYÊN TẮC CHỐT (user nhấn mạnh 2 lần): KHÔNG viết markup song song

User bực vì em thêm checkbox "Bên mua = người được BH chính" cho Motor bằng markup RIÊNG (khác align-items, margin, thêm title) → lệch style với Health.

=> QUY TẮC BẮT BUỘC khi làm UI: khi 1 element/khối xuất hiện ở >1 đối tượng/sản phẩm, PHẢI tách 1 helper/component DÙNG CHUNG, mọi nơi gọi ra; khác biệt chỉ là THAM SỐ trạng thái (checked/disabled/hidden/onchange/label). "Bản chất là component ẩn/hiện/khóa, không phải mỗi đối tượng 1 style." TUYỆT ĐỐI không copy-paste markup rồi chỉnh tay từng chỗ.

Đã sửa: thêm helper buyerInsuredToggle(checked,{disabled,onchange,label}) dùng ở Health(604)+PA(633)+Motor. Motor đặt trong card trắng padding:16 giống Health, checked+disabled. ?v=20260724h.

Các component dùng chung đã có: BANCA.docItemHtml (OCR doc row), buyerInsuredToggle (checkbox bên mua), qRow (dòng báo giá), caseStepperStrip (thanh tiến trình tracking), CUSTOMER_INFO branch (card khách hàng). LUÔN tái dùng, không vẽ lại.

## 2026-07-24 — Bỏ dòng meta (Việc tiếp theo/SLA/Phiên bản/Owner) trong header tracking

User: bỏ luôn dòng meta inline em thêm ở header. Xóa div meta, giữ hàng nút (getSubmittedCaseActions) căn phải (justify-content:flex-end). ?v=20260724i.

(Ghi chú phụ: tab Thanh toán có "Liên kết đã gửi: undefined" — bug nhỏ có sẵn, chưa trong scope, để sau nếu user yêu cầu.)

## 2026-07-24 — QA/QC: đồng bộ logic tiến độ luồng xử lý (single source of truth)

Bug user báo: hồ sơ đã auto thẩm định (STP, đang Chờ thanh toán) nhưng section "Tiến trình" vẫn "Thẩm định (đang chờ)".

NGUYÊN NHÂN GỐC: 3 component tiến độ suy trạng thái từ NGUỒN KHÁC NHAU:
- header stepper (caseStepperStrip): dùng caseView.states — ĐÚNG.
- timeline() "Tiến trình": dùng app.uw/app.confirm/app.payment (field cũ của Motor) → health STP không có app.uw → "Thẩm định đang chờ" SAI.
- cardStatus "Trạng thái nghiệp vụ": dùng danh sách chuỗi st (app.status) hardcode → state PAYMENT_METHOD_REQUIRED cho ○Thẩm định + ✓Chờ khách (mâu thuẫn).

FIX: tạo caseFlow (SINGLE SOURCE OF TRUTH) ngay sau caseView, derive từ caseView.states {uwDecided, needConfirm, confirmComplete, confirmDone, paySuccess, payProcessing, issued, declined, cancelled, dead}. Cả 3 component (caseStepperStrip + timeline + cardStatus nghiệp vụ) đều đọc từ caseFlow. "Chờ khách" done = confirmDone && uwDecided (tránh done trước khi thẩm định).

Verify live: HLT5 (payment pending) → Thẩm định ✓ / Thanh toán chờ. HLT3 (approved chưa đủ confirm) → ✓Thẩm định ○Chờ khách (khớp). HLT1 (issued) → all ✓. 3 view khớp nhau + khớp stepper. ?v=20260724j.

BÀI HỌC: trạng thái/tiến độ PHẢI derive từ 1 resolver chung (caseView.states/caseFlow), KHÔNG mỗi component tự suy từ field/status string riêng — đó là nguồn lỗi "vặt về logic" xuyên suốt.
